destruct(3)           LPC 系統函數           destruct(3)

名稱
     destruct() - 把某物件從記憶體中除去

語法
     void destruct( object ob );

描述
     把物件 <ob> 從記憶體中除去.
     注意, 如果 <ob> == this_object(), 程式不會因此而中止執行,
           不過, 這樣也許會造成混淆, 因此勸您在此狀況下用本函
           數的話, 能立刻返回.

請參考
     clone_object(3), new(3), destruct_env_of(4), move(4)

MudOS                Last change: 5 Sep 1994                    1

