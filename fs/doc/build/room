□ 如何建造房間

    首先﹐讓我們來解釋一下「房間」這個物件﹐幾乎和其他所有的 LPmud
一樣﹐我們提供一個房間的標準物件用來所有的房間繼承﹐然後你必須寫一
個 create() 函數來設定房間的敘述、出口、門、怪物、物品等。

    看起來很簡單吧﹐沒錯﹐就是這麼簡單﹐在這裡也不舉例說明了﹐你只
要用

    more here
    cat here

    就可以查看你目前所在房間的程式﹐製作房間是最基本的﹐如果你從來
沒有在 LPmud 中寫過程式﹐建議你從自己的 workroom 開始 ﹐寫一個簡單
的房間。
