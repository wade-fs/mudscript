LPC 進階－３　最佳化程式碼

一、佳乎？不佳乎？

  在討論最佳化程式碼的時候，有個詞需要先給它定義一下：什麼是好的程式
碼？相信這個問題的答案人人不同，但相信異中有同：

  一段好的程式碼是要能得到正確答案外，還能容易修改，必要時還要不同人
也能容易閱讀與修改，當然，還要盡量不佔用系統負載。

  我們得承認，我覺得是好的程式碼，也許別人不這麼想，儘管如此，這裡還
是有一些標準可供參考，希望能幫助讀者早日步入資深程式設計師的行列。

二、風格

  雖然程式是由程式設計師寫出來的，但是我們一直要強調一件事，程式不是
只有設計者會去讀、修改，尤其在泥巴中。因此，與其討論風格不如早點討論
最佳化程式碼本身不是嗎？事實上正因為在泥巴中程式碼是共享的，風格才會
顯得更重要，此處的風格不再只是程式者本身的，而是程式碼的風格。

  有人說資深程式師會用 %20  的時間寫出 80%  的程式碼來，剩下 80%  的
時間則寫那 20%  的部份與除錯。這似乎有點輕視程式師的能力，不過，當我
們看普通程式師如何寫程式會發現：他們用 80%  的時間只寫了 20%，而且那
20% 的程式讓他們發現裡頭一大堆錯誤，所以他們回過頭來又花了 80%  的時
間除錯，到最後甚至產生不了一個正確的程式碼來。

  要想用 20%  的時間很快的寫出正確而優良的 80%  的程式碼，最重要的事
就是建立程式風格。不囉嗦，底下舉例，請讀者們先別急著了解程式碼：

  for(i=0;i<j;i++){printf("the value is %d\n",i);
  }printf("and, the max value is %d\n",j);

  讓我們比較下面一樣功能的碼：

  for (value = 0; value < MAX; value++)
  {
    printf ("the value is %d\n");
  }
  printf ("and, the max value is %d\n", MAX);

  我們已經用最簡單的例子來說明風格，不良風格的碼不但閱讀不易，除錯困
難外，往後要維修也是很困難。不過，所謂風格就是特色，是沒有一定的標準
可言，不過仍然有規則可尋：

  １、縮排

　　上例就是一個好例子，第一個 printf() 是 for  迴圈內的敘述，因此縮
排進去，而第二個 printf() 則地位跟 for  相同，因此欄位相同。不過，縮
排多少空白倒沒有一定結論，有人喜歡用定位字元（Tab）， 有人縮兩格，有
人縮三格，我想這就是所謂的風格吧。

    什麼時候要縮排？我想上例也顯示了原則：以敘述所佔的地位縮排。通常
是 if/else，while，switch，for  等等，底下再舉例，先看沒適當縮排得：
  
    if (a > b) {
    if (c > d) {
    printf ("c > d\n");
    }
    else {
    printf ("c <= d\n");
    }
    printf ("a > b\n");
    }
    else {
    printf ("a <= b\n");
    }

    底下是有縮排的：
  
    if (a > b) {
        if (c > d) {
            printf ("c > d\n");
        }
        else {
            printf ("c <= d\n");
        }
        printf ("a > b\n");
    }
    else {
        printf ("a <= b\n");
    }
  
    相信大家能同後者較容易閱讀吧！

  ２、配對括號

　　讓我們再看一段跟同上一例的不同風格：

    if (a > b)
    {
        if (c > d)
	{
            printf ("c > d\n");
        }
        else
	{
            printf ("c <= d\n");
        }
        printf ("a > b\n");
    }
    else
    {
        printf ("a <= b\n");
    }
  
    這兩種風格誰好誰壞我們不給定論，由讀者自行選擇。
    不過我們再看另一例子：

    if (((a>b) && (c>d)) || ((a>c) && (b>d))) win();

    與：

    if ( ( (a > b) && (c > d) ) || ( (a > c) && (b > d) ) )
    {
      win();
    }

    與：

    if ( ((a>b) && (c>d)) || ((a>c) && (b>d)) )
      win();

    那一個較容易閱讀？我是比較傾向於第三者，這背後有作者個人的理由，
事實上也是風格的特色。我的想法如下：
    &&  運算子是要同時成立的敘述，因此可以看成同一個敘述，我就盡量將之
放在一起，而 || 運算子可看成不同敘述，所以就放開一點。而 win()  已經不
是 if 敘述的一部份，而且是附在 if 下的，所以換行且縮排。另外，之所以不
選第二種狀況給予一對大括號是因為， if 下的敘述只有 win(); 縮排已經可以
突顯出來。或許有人喜歡下列第四種狀況：

    if ( ((a>b) && (c>d)) || ((a>c) && (b>d)) )
    {
      win();
    }

    當然，這得由讀者自行選擇。我們看看多行的例子：

    if (x > y) {
      z = x + y;
      y += 2;
    }

    跟

    if (x > y)
    {
      z = x + y;
      y += 2;
    }

    此例中，也許出乎意料，我是選前者，沒必要左右大括號要同一欄，前者
一樣可以由右大括號知道 if 敘述已經結束，且可以省一行。

  ３、白空

    for(i=0;i<100;i+=2)printf("%d\n",i);

    這是印出 0 到 100 間的偶數的程式碼，跟下面的碼比較看看：

    for ( i=0; i<100; i+=2 )
      printf ("%d\n", i);

    及

    for ( i = 0; i < 100; i += 2)
      printf ( "%d\n", i );

    第一種空白太少，而第三種又太多，這是一種感覺，由讀者自行揣摸。
    適當的空白可以增加可讀性，立即效果就是不易傷腦筋，自然寫得快囉。

    白空不止是空白，還包括定位字元（Tab）、 換行，我們希望能多利用白
空提昇程式的可讀性，也就是塑造程式風格。

  ４、命名

　　變數與函數的名稱如果太簡單，容易喪失可讀性，日後可能無從了解程式
的意義，我們舉例來看：

    int i,j;
    string k="Hello, world.\n";

    j = strlen (k);
    for (i=0; i<j; i++)
      if (k[i] <= 123 && k[i] >= 98)
        k[i] = k[i] - 32;

    上一段會把小寫轉成大寫，讓我們比較下面一段相同功能的碼：

    int		i, len;
    string	msg = "Hello, world.\n";

    len = strlen (msg);
    for (i=0; i<len; i++)
      if (msg[i] <= 'z' && msg[i] >= 'a')
        msg[i] = msg[i] - ('a' - 'A');

  ５、註解

　　大部份的人都會認為註解太麻煩，與而且還不急嘛！有這種想法的人大部
份都很少真正用心在寫註解。雖然把註解擺在最後，並不是因為不重要，而是
希望讀者能記得更牢靠。事實上，註解不止是風格的一部份，說嚴重點，它可
說是與程式師的德性有關。勤於寫註解的人一般來說，較注重完美，與重視他
人的感受。不過，註解太多有可能造成閱讀者的困擾，這一點我們無法提供標
準，只能說，在適當的地方寫註解是程式師的責任。

    底下有一些參考：

    檔頭：包含程式目的、語法（用法）、原創者（沿革）、日期、資料型態
　　函數原型：應宣告所有函數前面，通常是在整體變數後面。
