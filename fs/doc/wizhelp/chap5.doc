第五章：：
　　　　　　　　　　ＥＤＩＴ(線上編輯器的使用)       　　


前　言：
        經過這幾天的說明，相信大家慢慢進入狀況了。其實我們不論編寫物件
或編寫人物，除了用FTP在自己的電腦上傳外，也可以使用線上編輯器(EDIT)，
來編寫，學好EDIT是一個巫師要做的一件事，因為他可以讓你節省很多的時間，
可能小小的BUG.你可以不用在重傳直接在線上就修改了...

說  明：
        以下的內容將帶你一步一步的使用及熟練EDIT的用法.......
一、EDIT的位置::
        只要你是Apprentice的wiz就可以使用線上的編輯器.......
二、Edit的指令說明::
以下是edit所有的指令說明...
命令
--------
/       往後搜尋一個 pattern
?       往前搜尋一個 pattern
=       顯示目前的行號
a       在本行後面開始插入文件
A       類似 'a', 但是有 inverse autoindent mode
c       改變現在所在的這一行 (取代掉)
d       刪除一行(或多行)
e       不編輯目前的檔案, 開始編輯另一檔(如目前的檔案已動過, 會失敗)
E       同 'e', 但是會強迫放棄已編輯的檔
f       顯示或更改現有的檔名
g       依據 pattern 在配對到的行執行某些命令.
h       顯示輔助訊息
i       在這行的前面開始插入文件(跟'a'不一樣, 但是類似)
I       indent the entire code (Qixx version 1.0)
j       把底下一行連到現在這一行, 兩行變一行
I       indent the entire code (Qixx version 1.0)
j       把底下一行連到現在這一行, 兩行變一行
k       用一個字元標記這一行(為了以後方便跳到這行) - later referenced as 'a
l       顯示本行內容
m       把本行移到特定的行號去
n       顯示行號或不要
O       同 'i'
o       同 'a'
p       顯示某範圍的文件內容
q       離開 edit (如果該文件有被動過, 會失敗)
Q       強迫離開編輯本文件
r       讀進另一檔到現有行的後面, 兩檔合併用
s       搜尋與取代
set     查詢與設定系統的設定值
t       拷貝某一行(或某些行)到特定的行號去, 請參考 'm'
v       依據 pattern 在沒配對到的行執行某些命令,請參考 'g'
x       同時存檔與離開編輯
w       寫入該檔(或所指定的檔名)
W       類似 'w', 但是是"附加"在已有的檔後面, 不是完全取代掉
z       顯示 20 行, 可以用 +/- 來控制方向
Z       類似 'z', 但是是 40 行

三、EDIT的用法::
        用法其實很簡單.只要打

1. EDIT <檔案的名稱>   //就行了.記得要打副檔名(如果有的話)
2. 如果是舊檔案你可以使用 z 或 Z 來看檔案(比more方便)。
3. 如果要編輯新檔案, 請先打 a 或 i 或 c :
       a = 從此列之下插入編輯;
       i = 從此列之上插入編輯;
       c = 修改此列並插入編輯。
4. 打入數字則會跳至那列並輸出內容。
5. 如果你編輯完畢, 請在編輯結尾的次列開頭處打 . 即可。
6. 如果想存檔請打 x 。 否則請打 q 或 Q 。
7. 如果你想知道現在是第幾列請打 = , 想知道內容請打 p 。
   或者可以先 set number 然後 set save, 則以後你線上編輯
   時, 會自動在開頭處印出列數。
8. 其餘功能可打 h 參考。
9. 又數字指令及非數字指令可合併使用。如 :
       3d = 刪去第 3 列;
       2,10d = 刪去第 2～10 列;
       5,8m1 = 將第 5～8 列移至原第 1 列之下。
如果打h的話..會出現!!!!!!
輔助訊息
  h       顯示輔助訊息
          想得到更詳細的輔助訊息請用 hc, 例如
  :hw       <-- 可以查 'w' 這個命令的詳細說明
搜尋/取代
  /       往後搜尋一個 pattern
  ?       往前搜尋一個 pattern
  g       整體搜尋 pattern 並顯示配對到的行
  s       搜尋與取代
  v       依據 pattern 在沒配對到的行執行某些命令,請參考 'g'
顯示
  =       顯示目前的行號
  l       顯示某範圍的文件內容(包括控制字元)
  p       顯示某範圍的文件內容
  z       顯示 20 行, 可以用 ./- 來控制方向
  Z       類似 'z', 但是是 40 行
輸入/刪除
  a       在本行後面開始插入文件
  W       類似 'w', 但是是"附加"在已有的檔後面, 不是完全取代掉到某行
  k       用一個字元標記這一行(為了以後方便跳到這行) - 以後可以用 'a 跳到那行
          去
  :10       <-- 跳到第 10 行, 當然要按 enter 之後生效
系統設定
  n       顯示行號或不要
  set     查詢與設定系統的設定值
四、實例操作::
  說了那麼多..若還不了解.沒關系.我們來實例操作..............
  如下....
  /u/c/chan/edit test.c  編輯的檔案的名稱
  isn't readable 進入edit的編輯的環境，表示這個檔案是新開的
  :              進入命令的模式
  :n
  number on, list off 表示打開行號表示
  :a             若新開的檔案從頭開始編輯
     1. 我是天下高手...   輸入第一行的內容
     2. .                 按.表示結束輸入
  :     回到命令模式
  :
   End of file.表示到了檔案的最後
  :x           存檔並寫入
"u/c/chan/super" 1 lines 16 bytes  你共輸入一行,共16 bytes的空間
/u/c/chan> more super
我是天下高手...   你就可以看到你輸入的內容了...
---------------------------------------------------------------------------
/u/c/chan> edit super 寫以經有的file
:
:n
number on, list off
:1
  1  我是天下高手...
:
End of file.
:c                     更改1.的內容
  1  chan是大帥哥..
:.
:x
"u/c/chan/super" 1 lines 16 bytes
Exit from ed.
/u/c/chan> more super
chan是大帥哥..   你就可以看到你更改的內容了...
以上就是Edit的用法.相信你一定學會了吧....
注意::
     若你是使用tintin程式的話.記得不要用edit.因為上引號會出不來..就會影響很大

                                                             98/07/19 by Chan
注意::以上文件..僅供狂想空間、南站的wiz使用..未經Chan允許不得流出..否則..

