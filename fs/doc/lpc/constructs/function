* LPC 函數(或者稱為 "方法")

The LPC function is similar but not identical to that provided by C
(it is most similar to that provided by ANSI C).  The syntax is as follows:
    LPC 的函數很類似 C, 但是又不完全一樣(最像的是 ANSI C).
  一般的語法如下:

傳回的資料型態  函數名稱(資料型態 參數-1, 資料型態 參數-2, ...)
{
	// 變數宣告在前頭, 這是一般的方式.
	資料型態 變數-1;
	資料型態 變數-2;
	...;

	敘述;
	...;

	return 函數的傳回值;	// 請配合前面宣告時的資料型態
}

    雖然註解裡有說了, 但是在此再次強調, 傳回值要跟前面的宣告
  的資料型態一致. 假如你用 void 來宣告函數的傳回型態, 那就是
  代表不必傳回值, 可以直接用 return; 這樣結束函數. 例:

	void 函數名稱(資料型態 參數, ...)
	{
	  敘述;
	  ...;
	}

    呼叫函數的方法要配合函數的宣告, 如下面的樣子:
	函數名稱(參數-1, 參數-2, ...);

    此外, 可以呼叫別的物件中的函數, 方法如下:
	物件->函數名稱(參數-1, 參數-2, ...);

    或:
	call_other(物件, 函數名稱, 參數-1, 參數-2, ...);

    最後, 最新版的 MudOS 有支援函數型態的變數, 請見變數型態的說明.
