BBS 系統？有點誇大，不過，如果有能人能接收 NEWS 的話，也不是不可能，
只是現在能人也很少出來做事了，一想到這兒，我們只好再次向 annihilator 致意！
我也只能寫寫 mud 內的 BBS 罷了！

首先：我們提供類似的功能，只是要做的事比平常的 BBS 麻煩，第一步，你要用
      select 來選要看的版面，但是，有哪些版面？底下兩個都可以，一樣功能：
      select cat
      list cat
其次，看版面有哪些佈告的話，請用 list, 這個命令敘述如下，自行參考。
      看版的話，就要用 "read" 命令。
      不過，再繼續說下去之前，要告訴大家的是，每個佈告有個序號，只有在讀的
      時候才會改變，如果你想跳到後面去讀的話，list 要加數字，或者用
      renew 命令改變一下，告訴系統，你設成上次看到 renew 指定的地方
當然，還有精華區，不過，等到以後再說好了。

============================================================================
list
============================================================================
指令格式 : list [cat | [[+|-]數字N]
BBS 說明 :
        此為 BBS 系統，命令有：
        使用者：list, select, post, read, renew
        巫　師：discard, mark, extract
指令說明：
        查看佈告。
        list            <-- 若未使用 select 選擇看哪一個版面，則同 list cat
        list cat        <-- 列出所有版面
        list N          <-- 從起點往後跳至 N 個討論後，列出其後二十個討論
        list -N         <-- 從起點往前跳至 N 個討論後，列出其後二十個討論

============================================================================
post
============================================================================
格式 : post <標題>
指令說明 :
           本命令用來貼布告，是採用系統提供的編輯器，
        跟一般巫師們在編輯檔案時用的編輯器一樣，如:
           post test

其他參考 :
      1. 在貼布告時，可在冒號模式下按 h 取得線上說明。
      2. 本命令可以配合簽名檔使用，請參考 sig 命令。

============================================================================
read
============================================================================
指令格式 : read #
指令說明 :
        用來讀第 # 封佈告，請參考 list, post, discard 等命令。



============================================================================
renew
============================================================================
指令格式 : renew [$|#]
BBS 說明 :
        此為 BBS 系統，命令有：
        使用者：list, select, post, read, renew
        巫　師：discard, mark, extract
指令說明：
        重設指標，也就是下次預設從哪一個佈告開始。
        renew           跳至最前面
        renew $         跳至最後面
        renre #         跳至第 # 封

============================================================================
select
============================================================================
指令格式 : select [cat|版面]
意義 :
        此為 BBS 系統，命令有：
        使用者：list, select, post, read, renew
        巫　師：discard, mark, extract
指令說明：
        選擇要看哪一個版面。
        select          <-- 如已設定，印出目前的設定，否則提示一下。
        select cat      <-- 列出所有版面
        select 版面     <-- 一看就知道了吧！

============================================================================
mark
============================================================================
指令格式 : mark [[N][-][M]]
意義 :
        此為 BBS 系統，命令有：
        使用者：list, select, post, read, renew
        巫　師：discard, mark, extract
指令說明：
        標示佈告。
        mark            顯示所有被 mark 住的佈告
        mark N          第 N 封
        mark N-         標示第 N 封以後的
        mark -M         標示從頭至第 M 封
        mark N-M        標示第 N 至 M 封

============================================================================
discard
============================================================================
指令格式 : discard [[N][-][M]]
意義 :
        此為 BBS 系統，命令有：
        使用者：list, select, post, read, renew
        巫　師：discard, mark, extract
指令說明：
        刪除佈告。
        discard         太笨了，幫您顯示 本訊息。
        discard N       刪除第 N 封
        discard N-      刪除第 N 封以後的
        discard -M      刪除從頭至第 M 封
        discard N-M     刪除第 N 至 M 封
