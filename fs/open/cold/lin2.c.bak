// Room: /u/c/chhu/workroom.c
#include "../cold.h"

inherit ROOM;

void create()
{
	set("short","神殿內廳");
	set("long", @LONG
這裡是神殿的內廳﹐在裡頭﹐一片空蕩蕩的﹐沒有任何東西
﹐讓人感到懷疑這裡是否有什麼陰謀﹐或是有什麼特殊的秘密 ?
只見這邊有個開著的小窗﹐不時的吹著冷冷的北風﹐讓人不寒而
慄。

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"south":COLD_ROOM"lin1",
]));
	set("objects",([
	]));
	setup();
	replace_program(ROOM);
}
