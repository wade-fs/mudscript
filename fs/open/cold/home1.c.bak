// Room: /u/c/chhu/workroom.c
#include "../cold.h"

inherit ROOM;

void create()
{
	set("short","寶珍神宮大門");
	set("long", @LONG
這裡是寶珍神宮的大門﹐裡面直通大殿﹐而兩側通往側殿﹐
大殿裡是掌門平常管理神宮事務之所﹐而兩旁側殿則是一般弟子
居住之所。兩旁的大門直聳而立﹐如兩頭巨象站立一般﹐若非親
眼所見﹐實在不敢想像世間上有這種大門。

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"enter":COLD_ROOM"master_room",
	"west":COLD_ROOM"left1",
	"east":COLD_ROOM"right1",
]));
	set("objects",([
	COLD_NPC"trainee7":1,
	]));
	setup();
	replace_program(ROOM);
}
