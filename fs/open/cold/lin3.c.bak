// Room: /u/c/chhu/workroom.c
#include "../cold.h"

inherit ROOM;

void create()
{
	set("short","神龍道");
	set("long", @LONG
這裡是由神殿正廳通往非凡神間的通道﹐寒谷門人稱之為
「神龍道」﹐只是為何稱作「神龍道」尚未可知。

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"east":COLD_ROOM"lin1",
	"west":COLD_ROOM"lin5",
]));
	set("objects",([
	]));
	setup();
	replace_program(ROOM);
}
