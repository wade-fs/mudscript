// Room: /u/c/chhu/workroom.c
#include "../cold.h"

inherit ROOM;

void create()
{
	set("short","寶珍神宮西側側廳");
	set("long", @LONG
相對於東側側廳﹐這裡是西側的側廳﹐同樣的﹐這裡也是空空蕩蕩
的﹐只不過在牆上有著一幅對聯﹕

        「不知江東情懷﹐怎知寒谷情深 ?」

	「不懂青影迷思﹐難懂寶珍緣奇。」

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"east":COLD_ROOM"in1",
]));
	set("objects",([
	]));
	setup();
	replace_program(ROOM);
}
