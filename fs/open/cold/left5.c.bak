// Room: /u/c/chhu/workroom.c
#include "../cold.h"

inherit ROOM;

void create()
{
set("short","靜格小軒");
	set("long", @LONG
這裡是寒谷的「靜格小軒」﹐在寒谷一派開山祖師初來到寒谷之
時﹐尚未有寶珍神宮及行宮、神殿等﹐在那時寒谷一派的開山祖師便
是居住在此﹐名曰「靜格小軒」﹐在軒內雖然什麼簡陋﹐卻有著一股
前人深著的氣息存在。

LONG
	);
	set("exits", ([ /* sizeof() == 2 */
	"southeast":COLD_ROOM"left3",
]));
	set("objects",([
	]));
	setup();
	replace_program(ROOM);
}
