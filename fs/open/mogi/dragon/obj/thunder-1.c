//by casey

inherit ITEM;

void create()
{
        set_name("閃雷玉",({"thunder jade","jade"}));
        set_weight(30);
   set("long","這是一塊上等玉塊,上面有著閃電的刻紋 ,是用來通過結界用的道具。\n");
        set("unit","塊");
        set("no_sell",1);
        set("no_auc",1);
        set("no_drop",1);
        set("no_put",1);


   setup();
}

