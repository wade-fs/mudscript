#include <room.h>
inherit ROOM;
void create()
{
        set("short","羊棚");
        set("long", @LONG
  這裡是蒙古人飼養家畜的棚子，門邊的架上堆著許多乾草，
一桶水擺在門邊，看起來都是要餵給牲畜食用的，幾隻綿羊在
棚裡打盹兒，門口掛著一副搖鈴。
LONG
        );
        set("outdoors", 1);
        set("exits", ([ /* sizeof() == 4 */
        "east":__DIR__"grass09",     
        ]) );
        set("no_fight", 0);
        set("objects",([ 
        __DIR__"obj/ring" : 1,             
]));

  setup();
}


