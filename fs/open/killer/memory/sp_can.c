inherit ITEM;

void create()
{
        set_name("特製竹筒", ({ "special can","special","can" }) );
        set("unit", "支");
        set("long", "平凡無奇的竹筒，但你卻不知道要如何開啟，裡面似乎有存放東西\n");
        set_weight(100);
        set("value",0);
}

