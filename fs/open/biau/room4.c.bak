// Room: /u/o/onion/room/w_room2.c

inherit ROOM;

void create()
{
	set("short", "鑣局內堂走廊");
	set("long", @LONG
這裡已到了長廊的盡頭,再往前便是僕人工作的地方,鑣局內的廚房便在前方,不時
還傳來陣陣的菜香味,濃郁的味道引的你食指大動,肚子忍不住咕嚕咕嚕的叫了起來.
    南方則是僕人的臥房.

LONG
	);
	set("exits", ([ /* sizeof() == 3 */
  "south" : __DIR__"room5",
  "west" : __DIR__"room3",
  "east" : __DIR__"cookroom",
]));

	setup();
}
