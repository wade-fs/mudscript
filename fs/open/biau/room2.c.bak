// Room: /u/o/onion/room/r_room0.c

#include <room.h>

inherit ROOM;

void create()
{
	set("short", "管家房");
	set("long", @LONG
這裡是鑣局內的大管家所住的地方,大管家負責整間鑣局上上下下數十口的生活起居,
大小事情都需大管家的過目,因此地位也算不錯,整間房間佈置的一絲不茍,東西都整齊的
放置著,由此可知大管家的為人處世是多耿直了...
    北邊有一道木門(door).
LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"room1",
	]));
        set("item_desc", ([ 
        "door" : "一扇堅實的木門,上頭雕著複雜的花紋.\n",
        ]));
        set("objects",(["/u/o/onion/npc/steward" : 1]));
	setup();
        create_door("north","木門","south",DOOR_CLOSED);
}
