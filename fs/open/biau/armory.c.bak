// Room: /u/o/onion/room/armory.c

inherit ROOM;

void create()
{
	set("short", "鑣局練武場");
	set("long", @LONG
這裡三三兩兩到處都有許多人在此練功夫,拳腳棍棒,虎虎生風,各地各式的功夫
你都可以見到,每個人都賣力的練到滿身大汗,想到自己多麼的偷懶,不禁有一絲愧意.

LONG
	);
	set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"armory",
]));
	set("objects", (["/u/o/onion/npc/bully" : 3 ]));

	setup();
}
