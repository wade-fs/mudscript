inherit ROOM;
#include <ansi.h>
#include "ice.msg"
#include "icestorm.c"

void create()
{
        set("short",HIW"╖ее_жBнь"NOR);
        set("long", (: printf_msg :) );
        set("exits", ([
	"north"	: __DIR__"room04",
	"south"	: __DIR__"room06",
	"west"	: __DIR__"room21",
	"northeast"	: __DIR__"room32",
        ]));
        set("objects",([
                        __DIR__"npc/mob4" : 1,
        ]));
        set("light", 1);
	set("no_auc", 1);
	set("no_transmit",1);
	set("no_chome",1);
	set("no_quit",1);
	set("no_scale",1);
        setup();

}
