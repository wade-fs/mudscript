inherit ROOM;
#include <ansi.h>
#include "ice.msg"
#include "icestorm.c"

void create()
{
        set("short",HIW"╖ее_жBнь"NOR);
        set("long", (: printf_msg :) );
        set("exits", ([
	"northeast"	: __DIR__"room30",
	"southwest"	: __DIR__"room32",
        ]));
        set("light", 1);
	set("no_auc", 1);
	set("no_transmit",1);
	set("no_chome",1);
	set("no_quit",1);
	set("no_scale",1);
        setup();

}
