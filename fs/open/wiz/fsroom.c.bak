inherit ROOM;
#include <ansi.h>
void create ()
{
    set ("short", "狂想空間建言處");
	set( "build", 80 );
    set ("long", @LONG
這裡就是讓狂想的玩家用來發表對於狂想空間的建議的地方::
除了:幫派(clan)、門派(class)、技能(skill)請到適合的地
方之外,你對狂想的制度,人事,主架構有何建議的地方都可以
在這裏發表。

	狂想空間巫師工作分配表 (help area)

LONG);

    set("valid_startroom", 1);
    set("no_transmit", 1);
    set("exits", ([ /* sizeof() == 2 */
//	"south" : "/open/wiz/meqroom.c",
	"north" : "/open/common/room/inn",
      ]));
    set("light_up", 1);
    set("no_kill", 1);
    set("no_chome", 1);
    set("no_fight", 1);

    setup();
    call_other("/obj/board/fsstr_b","????");
}
