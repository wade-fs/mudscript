#include <room.h>

inherit ROOM;

void create ()
{
  set ("short", "區域討論室");
  set ("long", @LONG
這裡是巫師聊天討論的地方之一﹐如果巫師們討論跟區域間有關的事
    情, 多半會選擇在這兒 post, 例如跨 area 的 quests....
    當然, 如果有巫師想申請負責或增加某 area, 也是在這兒 post.
LONG);

  set("exits", ([ /* sizeof() == 3 */
  "north" : __DIR__"entrance",
  "cap" : __DIR__"caproom",
]));

  set("no_clean_up", 0);


  setup();
 call_other("/obj/board/area_b","???");
}
