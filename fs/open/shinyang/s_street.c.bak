// Room: /open/shinyang/s_street.c
#include <room.h>
inherit ROOM;

void create ()
{
  set ("short", "南出口城門");
  set ("long", @LONG
由這裡進入襄陽城，前面站著數位衛兵，正在作嚴密的檢查，防止蒙古
大軍的入侵。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "north" : __DIR__"5-1",
]));
  set("outdoors", "/open/shinyang");

  setup();

  create_door("north","南門大門","south",DOOR_OPENED);
}
