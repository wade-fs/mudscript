// Room: /u/w/whatup/area/shinyang/3-4.c
inherit ROOM;

void create ()
{
  set ("short", "民宅");
  set ("long", @LONG
這間屋子十分的簡潔，只有一張不大的床鋪和一把竹椅，看來屋
子的主人過著十分清貧的生活，真不知道這裡的主人是誰，想必生活
過的不是很好。
LONG);

  set("no_transmit", 1);
  set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/child" : 1,
  __DIR__"npc/woman" : 1,
]));
  set("exits", ([ /* sizeof() == 1 */
  "south" : __DIR__"3-3.c",
]));
  set("no_auc", 1);
  set("light_up", 1);

  setup();
}
