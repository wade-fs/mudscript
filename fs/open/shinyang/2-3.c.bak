// Room: /u/w/whatup/area/shinyang/2-3.c
inherit ROOM;

void create ()
{
  set ("short", "金華大道西");
  set ("long", @LONG
金華大道西側出現了許多的馬兒，原來是主人牽著正在採買一些
出塞必需的用品，好刀好劍如同大拍賣一樣的掛在店門口，小二用盡
力氣的吶喊，彷彿這些武器都不用錢的一樣，如此吸引了不少旅客駐
足停下來，敲打評論這些武器，看似否能找到一把趁手的武器。
LONG);

  set("no_transmit", 1);
  set("exits", ([ /* sizeof() == 3 */
  "west" : __DIR__"1-3.c",
  "east" : __DIR__"3-3.c",
  "south" : __DIR__"2-2.c",
]));
  set("no_auc", 1);
  set("outdoors", "/open");

  setup();
}
