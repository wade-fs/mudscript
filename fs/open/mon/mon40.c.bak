inherit ROOM;
#include <ansi.h>

void create ()
{
  set ("short", "長情峰");
  set ("long", @LONG
長白山著名的三峰之一，站在此地，視野所及的，只有美不勝收
能形容，盛傳古時候有一對神仙情侶曾在此相識相戀，所以長情峰因
此而得名，若要觀看更美的景物，你可以登上峰頂。
LONG);

  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/pig.c" : 1,
]));
  set("exits", ([ /* sizeof() == 2 */
  "east" : __DIR__"mon47",
  "stand" : __DIR__"mon48",
]));

  setup();
}
