inherit ROOM;
#include <ansi.h>

void create ()
{
        set ("short", "草原");
	set( "build", 3 );
        set ("long", @LONG
這裏是一片綠油油的草原，你真不敢相信，在這麼高的長白山上
竟然會有如此的一片草原，這裏原本是一個池子，但是池水經年累月
的，已經乾涸了一部份，而乾涸的部份卻長出了一片青綠的草原。
LONG);
        set("outdoors", "");
        set("exits", ([ /* sizeof() == 4 */
        "east"      : "/open/ghost-hole/g-h01",
        "west"      : __DIR__"mon56",
]));
set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/fly-dancer.c" : 1,
]));
        setup();
}
