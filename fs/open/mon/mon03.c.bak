inherit ROOM;
#include <ansi.h>

void create ()
{
        set ("short", "荒涼小徑");
        set ("long", @LONG
眼前的景像，讓你有些些的迷網了，因為你已快找不到前面的去
路了，四周的景像一片荒涼，不遠處的前方隱隱可以看到一間房子，
讓你振奮了精神繼續往前行走。
LONG);
        set("outdoors", "");
        set("exits", ([ /* sizeof() == 4 */
        "east" : __DIR__"mon02",
        "west" : __DIR__"mon04",
]));
        setup();
}

