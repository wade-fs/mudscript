// made by ccat
inherit COMBINED_ITEM;
void create()
{
        set_name("靈芝仙丹", ({"super pill","pill"}) );
	set("long",
"此物乃蜀中將軍自蜀山神峰採來之神藥,專克血氣失調.\n"+
"此要須經藥師調配(check item),方得食用(swallow pill).\n");
        set("unit", "盒");
        set("base_unit", "粒");
        set("no_sell",1);
	set("value",2500);
        set("base_weight", 50);
	set_amount(1);
	setup();
}

void init()
{
	if( this_player()==environment() )
	{
		add_action("swallow_pill", "swallow");
	}	
}

int swallow_pill(string arg)
{      
      if( (arg=="pill" || arg=="super pill") && (query("unit") == "盒") )
      {
	object user;
	user = this_player();
        message_vision("$N吃下一粒靈芝仙丹.\n", user);
        if( user->query_condition("ff_poison") )
          user->apply_condition("ff_poison",0);
	add_amount(-1);
        return 1;
       }
        return 0;
        
}
              
                           
