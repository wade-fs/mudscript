// Room: /u/w/whatup/peach/masterhome2.c
inherit ROOM;

void create ()
{
  set ("short", "村長家");
  set ("long", @LONG
這裡是村長的屋子，屋子裡沒什麼糧食，空盪燙的一片，只放著
幾張桌子和椅子，除此之外，什麼都沒有了。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "out" : __DIR__"master.c",
]));

  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/sheik2" : 1,
]));

  setup();
}
