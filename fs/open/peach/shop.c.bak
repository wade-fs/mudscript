// Room: /u/w/whatup/peach/shop
inherit SHOP;

void create ()
{
  set ("short", "鄉村小店");
  set ("long", @LONG
這是市集裡面的一個小商店，裡面好像會賣一些平常沒看過的東
西。左邊的牆上掛著幾個常用的家用工具，在老闆後面的地方放著幾
件看來是平常拿來種田的工具，上面的標題寫著店內東西的價格。然
而這間商店的人卻用異樣的眼光看著你，眼神好像透出一些恨意。
LONG);

  set("exits", ([ /* sizeof() == 1 */
  "east" : __DIR__"virage5.c",
]));

  set("objects", ([ /* sizeof() == 2 */
  __DIR__"npc/keeper.c" : 1,
  __DIR__"npc/customer.c" : 1,
]));


  setup();
}
