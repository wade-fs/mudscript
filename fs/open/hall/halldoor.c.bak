// Room: /open/capital/room/halldoor
inherit ROOM;

void create ()
{
  set ("short", "衙門大門口");
  set ("long", @LONG
你看到的是京都總理衙門，掌管一切非法事項，由於前朝有個大公
無私的鐵面包拯，遺風仍在，因此一切誣告在此乃事所必行。
LONG);

  set("exits", ([ /* sizeof() == 2 */
  "south" : "/open/capital/room/r33",
  "enter" : __DIR__"hall",
]));
  set("item_desc", ([ /* sizeof() == 2 */
  "sign" : "有怨抱怨，有仇報仇。",
  "door" : "京都總理衙門大門口",
]));
  set("outdoors", "/open/capital");

  setup();
}
