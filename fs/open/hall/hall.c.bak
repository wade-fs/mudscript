// Room: /open/hall/hall.c

inherit ROOM;

void create ()
{
  set ("short", "京都總理衙門廣場");
  set ("long", @LONG
這裡是京都總理衙門前廣場，兩扇朱紅色的大門，透著一股莊嚴肅
穆的氣氛!!門口左側有座以木頭搭成的『公佈欄』，在其上張貼著幾張
官方所公佈的一些規定文告<<paper>>，勸你最好是看一下....!!
LONG);

  set("exits", ([ /* sizeof() == 4 */
  "west" : __DIR__"way2",
  "north" : __DIR__"hall_2",
  "out" : __DIR__"halldoor",
  "east" : __DIR__"way1",
]) );

  set("item_desc", ([ /* sizeof() == 1 */
  "paper" : "    本府專辦個種誣告，凡年滿十六歲之男女﹐懷疑任何人有不法
  行為﹐均可檢舉(accuse)。",
]) );

  set("objects", ([ /* sizeof() == 1 */
  __DIR__"npc/garrison_2" : 2,
]) );


  setup();
}
