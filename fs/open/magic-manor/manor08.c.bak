#include <ansi.h>
inherit ROOM;

void create ()
{
	set ("short", "走道");
	set( "build", 2 );
	set ("long", @LONG
一個簡簡單單的走道，可以通往火靈塔和各個靈閣，周圍的景緻
也很宜人，讓你在這小小走道上稍微休息一下也是一個不錯的想法。
北邊是火靈道，你也可以感覺有陣陣的火之氣息傳向這裏。
LONG);

	set("no_auc", 1);
	set("no_transmit", 1);

	set("exits", ([ /* sizeof() == 3 */
	"north"	: __DIR__"manor22",
	"east"	: __DIR__"manor07",
	"west"	: __DIR__"manor09",
	]));

	set("outdoors", "/open/magic-manor");

	setup();
}
