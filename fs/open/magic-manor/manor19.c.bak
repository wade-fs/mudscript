#include <ansi.h>
inherit ROOM;

void create ()
{
	set ("short", "右靈水閣");
	set( "build", 43 );
	set ("long", @LONG
水閣中充滿了一股奇異的靈氣，靈氣中隱隱的含著水的氣息，在
水閣的裏側正上方一塊匾額題著「右靈水閣」，而且還是莊主親自題
字，足以顯得對此閣的主人之重視，透過水閣窗戶向外看過去可以隱
隱看到一個繚繞著漫天水氣的水靈塔。
LONG);

	set("no_auc", 1);
	set("no_transmit", 1);

	set("exits", ([ /* sizeof() == 3 */
	"south"	: __DIR__"manor03",
	]));

	set("objects", ([ /* sizeof() == 2 */
	__DIR__ "npc/right-lingin" : 1,
	]));

	set("light_up", 1);

	setup();
}
