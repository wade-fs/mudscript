#include <ansi.h>
inherit ROOM;

void create ()
{
	set ("short", "火靈道");
	set( "build", 2 );
	set ("long", @LONG
一條通往火靈塔必需要經過的道路，火靈塔那裏傳來的火之氣息
已經愈來愈濃厚，若非有一番修為的人來到此，也許會抵受不住
那火氣的猛烈侵襲。
LONG);

	set("no_auc", 1);
	set("no_transmit", 1);

	set("exits", ([ /* sizeof() == 3 */
	"north"	: __DIR__"manor23",
	"south"	: __DIR__"manor08",
	]));

	set("light_up", 1);
	set("outdoors", "/open/magic-manor");

	setup();
}
