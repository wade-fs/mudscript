#include <ansi.h>
inherit ROOM;

void create ()
{
	set ("short", "聚靈階");
	set( "build", 31 );
	set ("long", @LONG
登上聚靈階，可以感覺些微的靈氣緩緩朝你飄過來，再往前走就
可以抵達江湖上鮮為人知的聚靈山莊了。
LONG);

	set("no_auc", 1);
	set("no_transmit", 1);

	set("exits", ([ /* sizeof() == 3 */
	"north"	: __DIR__"manor49",
	"out"	: __DIR__"town/town44",
	]));

	set("outdoors", "/open/magic-manor");

	setup();
}
