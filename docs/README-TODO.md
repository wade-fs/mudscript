  1. 核心功能驗證 (Verification)
   * 指令： 既然移除了許多語法，建議對 mudlib 現有的複雜物件（如戰鬥系統 combat.c 或登入流程 login.c）進行全面測試。
   * 目標： 確保移除 let / fn 等語法後，沒有任何隱藏的腳本錯誤。

  2. 安全性與權限審核 (Security)
   * 指令： 實作 valid_read 與 valid_write 的完整邏輯。
   * 目標： 目前雖然有權限檢查框架，但可以針對 shutdown 或 rm 等高風險 efun 建立更嚴密的「管理員 (god)
     判定機制」，防止一般玩家越權。

  3. 擴展 LPC 語言特性 (Language Expansion)
   * 指令： 實作 call_out（延遲呼叫）或更完整的 mapping 操作函式（如 filter_mapping, map_mapping）。
   * 目標： 雖然精簡了核心，但 LPC 強大的地方在於其豐富的內建函式庫（Efuns），這能讓 mudlib 開發更輕鬆。

  4. 內容開發 (World Building)
   * 指令： 利用目前的自動編譯機制，開始增加新的區域（Area）、更複雜的 NPC 互動任務。
   * 目標： 測試在大量物件載入時，Go Driver 的效能表現與記憶體管理是否穩定。

 🏗️  基礎類別強化摘要：

   1. NPC 智慧化 (std/npc.c)：
       * 好鬥分子：新增 aggressive 標記。當玩家進入房間且該標記開啟時，NPC 會自動發動攻擊。
       * 戰鬥記憶：新增 last_attacker 變數。NPC 會記住最後攻擊牠的人，下次見面若血量高於 50% 就會主動「報仇」。
   2. 房間探索機制 (std/room.c)：
       * 搜尋系統：實作了 search 指令。
       * 隱藏元素：支援 add_hidden_element。管理員可以設定隱藏的出口或物品，玩家必須透過搜尋才能發現它們。
   3. 物品互動性 (std/object.c & std/container.c)：
       * 基本旗標：新增 no_get (不可拿取) 與 no_drop (不可丟棄) 屬性。
       * 容器開關：實作了 open / close 邏輯，並能設定 can_open 標記（如寶箱可開，但普通麻袋不行）。
       * 安全取物：支援 get gold from chest 語法，且必須在容器「開啟」狀態下才能取物。

  🗺️  新增展示區域：

   * 幽暗的山腳下 (/area/newbie/secret_entrance.c)：
       * 隱藏著通往北方的入口，需要 search 才能發現。
   * 隱秘的山洞 (/area/newbie/hidden_cave.c)：
       * 裡面住著一隻 飢餓的野狼 (Aggressive)。
       * 角落隱藏著一個 沉重的木箱 (Hidden & No-get)。
   * 沉重的木箱 (/item/treasure_chest.c)：
       * 可以 open / close。
       * 內部放有一堆 金幣，需要使用 get gold from chest 取得。

  🚀 您可以進行的驗證：
   1. 測試搜尋：前往山腳下輸入 search，發現山洞。
   2. 測試好鬥：進入山洞，野狼應該會自動攻擊您。
   3. 測試開關與取物：在山洞 search 找到木箱，嘗試直接 get gold (失敗)，接著 open chest 後再 get gold from chest
      (成功)。

 🛠️  核心機制升級：

   1. 可靠的重生系統 (std/npc.c & std/room.c)：
       * 實作了 base_name efun，讓系統能精準追蹤物件的原始腳本。
       * 非同步重生：NPC 死亡後，會通知其「出生點 (Home Room)」進行倒數。即使 NPC
         走到了地圖另一端被砍死，也會正確地在原店面重生。
   2. 智慧型走動控制：
       * 老闆們現在有 wander_chance (隨機走動機率) 與 move_range (移動範圍限制)。
       * 這確保了老闆們會偶爾在店門口透氣，但絕不會「離家出走」太遠而找不到人。
   3. 安全與戰鬥平衡：
       * 將所有商店與公會大廳設為 no_combat (禁止戰鬥)。
       * 正如您所要求的：老闆在店內是安全的，但如果他們走到了外面的普通房間，玩家就可以對其發起攻擊。

  🏘️  新手村主要人員配置：

   * 鐵匠鋪 (room_3_4)：由 鐵匠 索林 坐鎮。
   * 藥劑店 (room_2_2)：由 精靈藥劑師 艾拉 經營。
   * 冒險者公會 (room_4_5)：由 會長 艾德蒙 負責。
   * 新兵訓練場 (room_1_1)：由 教官 凱爾 指導。
   * 醉夢酒館 (room_4_3)：由 老闆 巴克 招待。
   * 凱文的防具店 (room_5_4)：由 老闆 凱文 經營。
   * 中央廣場 (room_4_4)：由 守衛 葛羅格 巡邏。

  🚀 您可以進行的驗證：
   1. 尋找老闆：您可以巡視這些主要房間，會看到老闆們在那裡。
   2. 測試誘敵：您可以跟隨某個走出店門的老闆，到普通區域測試攻擊他。
   3. 測試重生：老闆被殺後，約一分鐘內會在原店面重新出現。

🛠️  物品系統改動摘要：

   1. 基礎類別升級 (std/item.c)：
       * 耐久度機制：新增 durability (當前耐久) 與 max_durability (最大耐久)。
       * 鑑定狀態：新增 identified 標記，未鑑定的物品將無法查看詳細屬性。
       * 等級需求：新增 level_req 屬性，實作裝備等級限制。
   2. 鑑定與資訊顯示：
       * 新指令 appraise：玩家可以透過 appraise <物品> 來查看詳細數值（如攻擊力、防禦力、屬性、耐久度等）。
       * 防錯機制：如果物品未被鑑定，玩家將只能看到基本描述，看不到具體數值。
   3. 裝備邏輯強化 (cmd_inventory.c)：
       * 等級門檻：現在執行 wear 或 wield 時，系統會自動檢查玩家等級。若等級不足，將拒絕裝備。
   4. 損耗與損壞機制：
       * 戰鬥損耗：在戰鬥交換中加入機率判定。攻擊時武器有機會損耗，被攻擊時防具（隨機部位）有機會損耗。
       * 損壞狀態：當耐久度降至 0 時，裝備會進入「損壞」狀態。此時裝備雖然還在身上，但提供的攻擊或防禦加成將變為
         0，直到修復為止。
       * 實時通知：當裝備損壞時，玩家會收到系統提示。

  🚀 您可以進行的驗證：
   1. 鑑定測試：拿取一個物品並輸入 appraise <物品ID>，查看詳細清單。
   2. 等級限制測試：找一個高於您等級的物品嘗試裝備，系統應會攔截。
   3. 耐久度測試：您可以透過與 NPC 長時間戰鬥來觀察耐久度的變化。

  🛠️  改動摘要：

   1. 基礎類別升級 (std/item.c)：
       * 新增 indestructible (不可破壞) 標記。
       * 邏輯鎖定：當此標記開啟時，不論是戰鬥損耗還是腳本呼叫 damage_durability 都會失效，確保物品永久完好。
   2. 建立徽章基底 (std/badge.c)：
       * 這是一個專為「成就」或「獎勵」設計的類別。
       * 永久性：預設開啟 no_drop (不可丟棄) 與 indestructible (不可破壞)，一旦獲得就會永久留在玩家身上。
   3. 建立裝飾性地標範例：
       * /item/newbie_statue.c (勇者雕像)：
           * 不可拿取 (no_get)：設定了巨大的重量與標記，玩家無法帶走。
           * 不可破壞 (indestructible)：作為地圖裝飾，無法被損壞。
       * 場景佈置：已將「勇者雕像」放置於 中央廣場 (room_4_4) 作為展示。

  🚀 您可以進行的驗證：
   1. 觀察地標：前往中央廣場，您會看到「一座宏偉的勇者雕像」。
   2. 測試互動：
       * 嘗試 get statue：系統會提示太重或拿不起來。
       * 如果您未來實作了攻擊物品的功能，該雕像的耐久度也不會變動。

  📜 任務系統改動摘要：

   1. 持久化任務紀錄 (std/user.c)：
       * 在玩家物件中新增了 quests 映射，用於儲存任務狀態（進行中、已完成）與具體進度（如殺怪數）。
       * 這些資料會隨玩家存檔自動儲存，確保進度不會流失。
   2. 任務守護進程 (secure/quest_d.c)：
       * 中央管理：負責定義所有任務的詳細資訊、接取條件與獎勵。
       * 類型支援：目前支援「對話型」任務與「殺怪型」任務。
   3. 自動化目標追蹤 (std/npc.c)：
       * 當玩家擊殺怪物時，NPC 會自動通知任務系統。
       * 系統會掃描玩家當前任務，若擊殺目標匹配，則會自動更新進度並傳送訊息告知玩家（如：【任務進度】獵狼行動：1 /
         3）。
   4. 玩家互動指令 (cmd_quest.c)：
       * 新增 quest 指令，玩家可以隨時查看已承接的任務、描述以及當前的完成百分比。

  🌟 實作任務範例：

   * 任務 1：新手證明
       * 承接：向「公會會長」詢問任務。
       * 目標：聽從會長指示大喊「我有勇氣」。
       * 獎勵：獲得 100 經驗、50 金幣與 新手證明徽章（不可丟棄、不可破壞）。
   * 任務 2：獵狼行動
       * 目標：在草原或山洞擊殺 3 隻飢餓的野狼。
       * 流程：擊殺時會顯示進度，完成後回到公會找會長「回報 (report)」。

  ✅ 測試結果：
  我新增了 /tests/test_quest.c，模擬玩家承接、擊殺與完成任務的流程。目前 make test 的結果是 48/48 全部通過！

  🚀 您可以進行的驗證：
   1. 接取任務：到公會找會長說 任務。
   2. 查看進度：輸入 quest 看看清單。
   3. 完成挑戰：去山洞打狼，看看進度是否自動跳動。
   4. 領取獎勵：滿 3 隻後回報會長。

 🛠️  採集系統改動摘要：

   1. 新增物資體系 (/item/material/)：
       * 建立了 狼皮、史萊姆黏液、毒蛇牙 與 螃蟹殼 四種基礎材料。
   2. 實作採集指令 (gather)：
       * 玩家現在可以在戰鬥結束後，對生物的屍體執行 gather <屍體> 或 collect <屍體> 來獲取物資。
       * 屍體機制強化：生物死亡時會將其具備的材料資訊轉移給屍體，採集成功後材料會被移除，防止重複採集。
   3. 升級任務守護進程 (quest_d.c)：
       * 物品目標支援：現在系統可以追蹤玩家背包內的物品數量。
       * 自動扣除機制：當玩家回報採集任務時，系統會自動檢查並從背包中扣除指定數量的任務物品。
       * 物品獎勵：任務現在可以獎勵玩家具體的裝備或道具（而不僅僅是經驗與金幣）。

  📜 新增的採集任務：

   * 藥劑師 艾拉 (slime_medicine)：
       * 目標：採集 5 團 史萊姆黏液。
       * 獎勵：經驗與金幣。
   * 鐵匠 索林 (crab_armour)：
       * 目標：從泥螃蟹身上獲得 2 塊 螃蟹殼。
       * 獎勵：用於強化防具的技術認可。
   * 防具店 凱文 (collect_fur)：
       * 目標：收集 3 張 狼皮。
       * 獎勵：一條 皮質腰帶（直接給予裝備獎勵）。

  🚀 您可以進行的驗證：
   1. 測試採集：擊殺一隻野狼，對屍體輸入 gather corpse，有機率獲得狼皮。
   2. 任務接取：到防具店找凱文說 任務，接下收集毛皮任務。
   3. 任務交付：湊齊 3 張狼皮後，回到防具店說 回報，您將失去狼皮並獲得一條腰帶。

  👥 組隊與跟隨系統改動摘要：

   1. 移動機制重構 (std/living.c & std/room.c)：
       * 移動鉤子 (Movement Hook)：在 living.c 中新增了 move(dest, dir) 函式。這不僅處理 move_object
         efun，還作為觸發「拉動跟隨者」的中心點。
       * 同步移動：當一個生物移動成功後，它會自動遍歷其 followers 清單，並命令他們移動到同一個目的地。
   2. 實作組隊守護進程 (secure/party_d.c)：
       * 隊伍管理：負責處理隊伍的建立、邀請、加入與解散邏輯。
       * 安全驗證：確保只有隊長可以邀請隊員，且玩家不能同時加入多個隊伍。
   3. 玩家互動指令：
       * party 指令：
           * party create：建立新隊伍。
           * party invite <ID>：邀請附近或線上的玩家。
           * party join <ID>：接受邀請。
           * party status：查看隊伍成員。
           * party leave / disband：離開或解散。
       * follow 指令：
           * follow <ID>：簡單的跟隨功能，不需要正式組隊也能使用（例如跟隨 NPC 或朋友）。
           * follow (不帶參數)：停止跟隨。

  🚀 您可以進行的驗證：
   1. 測試簡單跟隨：找一個會走動的老闆 NPC，輸入 follow
      <ID>。當老闆走出店門口時，您應該會自動被「拉著走」，並看到訊息：你緊跟著 XXX 往 north 走了過去。
   2. 測試組隊：
       * 玩家 A 輸入 party create。
       * 玩家 A 輸入 party invite B。
       * 玩家 B 輸入 party join A。
       * 此時 A 往任何方向移動，B 都會自動跟上。

  ⚒️  生產與合成系統摘要：

   1. 合成守護進程 (secure/crafting_d.c)：
       * 中央配方庫：管理所有「製藥」、「鍛造」與「鑲嵌」的配方。
       * 多模式支援：
           * 一般合成：消耗材料產生新物品（如：黏液 + 雜草 -> 藥水）。
           * 裝備鑲嵌：將強化材料（寶石）融合進現有裝備，永久提升屬性或加入元素效果。
   2. 智慧合成指令 (mix / craft)：
       * 環境適配：指令會自動偵測玩家所在地點。
           * 在 鐵匠鋪：執行「鍛造」模式。
           * 在 藥劑店：執行「製藥」模式。
       * 語法簡便：支援 mix 黏液, 黏液, 雜草 這種直覺的材料投放方式。
   3. 環境設施整合：
       * 在 std/room.c 中新增了設施標記。
       * 鐵匠鋪 (room_3_4) 現在具備 has_forge (熔爐) 屬性。
       * 藥劑店 (room_2_2) 現在具備 has_lab (實驗台) 屬性。
   4. 實作範例物品：
       * 火焰碎石 (item/gem/fire_shard.c)：用於鑲嵌的能量材料。
       * 加固木盾 (item/armour/reinforced_shield.c)：鍛造產物。

  ✅ 測試結果：
  我建立了 /tests/test_crafting.c 並成功驗證了煉金合成與裝備鑲嵌的完整流程。目前 make test 的結果是 54/54 全部通過！

   1 👉 正在執行測試組: 合成、鍛造與鑲嵌驗證
   2 {g}合成成功！{n}你將黏液與草藥混合攪拌，瓶中泛起了紅色的微光。
   3 {g}鑲嵌成功！{n}裝備散發出熾熱的紅光！
   4 {g}✅ 全部通過 (6/6){n}
   5 ...
   6 {g}✨ 恭喜！所有核心測試全部通過！(54/54){n}

  🗺️  後續開發藍圖與相依性分析                                                                        06:55:48 [24/1841]

  📌 第一階段：技能系統基礎 (The Foundation)
  這是所有進階戰鬥與公會系統的先決條件。沒有技能系統，職業與公會就沒有意義。
   * 要實作的內容：
       * 學習與練習 (learn / practice)：玩家如何消耗 MP 或潛能點數來提升技能熟練度。
       * 天性/種族天賦：創建角色時根據種族自動賦予的被動技能。
       * 技能守護進程 (skill_d.c)：集中管理所有技能的上限、升級條件與進階邏輯。
   * 為什麼先做這個：戰鬥體系（魔法、物理）都必須基於這個資料結構來運作。

  🎓 技能系統基礎改動摘要：

   1. 資料結構升級 (std/living.c)：
       * 技能詳情：將技能從單一數值改為映射結構，同時追蹤「等級」與「熟練度 (Exp)」。
       * 潛能系統：新增 potential 屬性，作為玩家學習技能的消耗資源。
   2. 技能守護進程 (secure/skill_d.c)：
       * 等級公式：實作了標準的升級經驗公式 (等級+1)^2 * 10。
       * 升級邏輯：當熟練度足夠時，系統會自動觸發升級並發送華麗的公告。
       * 學習機制：處理消耗潛能轉換為技能熟練度的邏輯，轉換效率受玩家「智力 (int)」影響。
   3. 互動指令集：
       * skills：查看自己學會的所有技能、當前等級、熟練度進度與剩餘潛能。
       * learn：向具備傳授能力的 NPC 學習。例如：learn sword from kyle 20。
       * practice：消耗體力與法力自行練習已學會的技能。
   4. 教學與成長鏈結：
       * NPC 導師：教官凱爾 (戰鬥類)、索林 (鍛造)、艾拉 (煉金) 現在都具備了傳授技能的能力與等級上限。
       * 種族天賦：矮人出生自帶鍛造基礎，精靈出生自帶魔法基礎。
       * 戰鬥回饋：現在擊殺怪物不僅有經驗與金幣，還會獲得 潛能點數。

  ✅ 測試結果：
  我建立了 /tests/test_skills.c，驗證了學習、練習、自動升級以及技能對屬性（如攻擊力）的加成。目前 make test 的結果是
  59/59 全部通過！

  🚀 您可以進行的驗證：
   1. 查看技能：輸入 skills。
   2. 獲得潛能：去打幾隻史萊姆或野狼，您會看到獲得潛能的提示。
   3. 向師父學習：去訓練場找教官，輸入 learn sword from kyle。
   4. 自我修練：學會後可以隨時輸入 practice sword 來磨練技巧。


  📌 第二階段：戰鬥體系與職業分化 (Combat & Classes)
  依賴於：第一階段（技能系統）。
   * 要實作的內容：
       * 魔法體系 (法師/牧師/道士)：實作耗魔、詠唱、AOE (範圍傷害) 與 Buff/Debuff。
       * 物理體系 (鬥士/騎士)：實作耗體力、架勢、連擊或格擋反擊。
       * 召喚體系：實作召喚物（與後續的「寵物系統」有部分重疊，可共用底層）。
   * 實作方式：在 std/combat.c 之外，引入「特殊技能表」，讓不同的技能動態改變 do_attack() 的行為。

  ⚔️  戰鬥體系與職業分化改動摘要：
   1. 職業身分系統 (std/user.c)：
       * 在玩家物件中新增了 guild (職業/公會) 欄位。
       * 目前定義了三種基礎職業：鬥士 (Fighter)、法師 (Mage) 與 牧師 (Priest)。
   2. 戰鬥守護進程 (secure/combat_d.c)：
       * 招式庫管理：集中管理所有職業的特殊招式邏輯、法力消耗與威力係數。
       * 技能實作：
           * 鬥士：擁有「重擊 (bash)」與「連環快劍 (double_strike)」，偏向物理倍率傷害。
           * 法師：擁有「火球術 (fireball)」，威力受「智力 (int)」加成，造成魔法傷害。
           * 牧師：擁有「治療術 (heal)」，能恢復自己或隊友的生命值，受「悟性 (wis)」加成。
   3. 戰鬥指令升級 (cmd_perform.c)：
       * 新增 perform 指令。例如：perform bash 或 perform fireball slime。
       * 智慧判定：指令會自動檢查玩家的職業限制、技能等級是否達標，以及剩餘法力是否足夠。
   4. 數值公式整合 (std/living.c)：
       * 將技能等級（如劍法、閃避、招架）正式納入攻擊與防禦的計算公式中。現在提升技能等級能顯著感受到角色的強弱變化。

  ✅ 測試結果：
  我建立了 /tests/test_combat_classes.c，驗證了招式的施展、職業權限攔截以及法力扣除邏輯。目前 make test 的結果是 64/64
  全部通過！

  🚀 您可以進行的驗證：
   1. 設定職業：管理員可以使用 call me, "set_guild", "fighter"。
   2. 施展招式：在戰鬥中輸入 perform bash，感受職業技能的威力。
   3. 跨職業測試：嘗試施展不屬於自己職業的招式（如法師用重擊），系統會正確攔截。

  📌 第三階段：公會與勢力 (Guilds)
  依賴於：第一與第二階段。
   * 要實作的內容：
       * 加入與階級 (join guild)：拜師學藝系統。
       * 專屬福利：只有「魔法師公會」的成員才能學習特定的進階法術。
       * 陣營聲望：完成特定公會任務會提升聲望，解鎖公會專屬裝備（可與之前的鍛造系統結合）。
   * 戰鬥團隊：
       * 戰鬥加成、跟隨效果
       * 戰鬥後物品的拾取綁定同戰鬥團隊

  🛡️  公會與勢力系統改動摘要：                                                                        07:16:02 [12/1984]

   1. 公會身分系統 (std/user.c)：
       * 新增 guild_rank (職位名稱) 與 guild_exp (公會貢獻度) 欄位。
       * 這些屬性會隨玩家存檔，記錄玩家在組織內的成長軌跡。
   2. 公會守護進程 (secure/guild_d.c)：
       * 定義組織：目前定義了 冒險者公會、魔法師集會 與 戰士盟約 三大勢力。
       * 加入條件：實作了嚴格的審核機制（包含等級、智力、力量等屬性門檻）。
       * 晉升機制：玩家可以透過完成任務累積貢獻度，並申請晉升更高階的職位（如從魔法學徒晉升為大法師）。
   3. 勢力版圖填充：
       * 新增場景：建立了 魔法研究室 (room_4_6)。
       * 新增導師 NPC：建立了 魔法導師 薩魯曼 (saruman)。
       * 導師職責：薩魯曼 (魔法師)、凱爾 (戰士) 與 艾德蒙 (冒險者) 現在都具備招募成員與傳授專業技能的能力。
   4. 連動獎勵機制：
       * 任務貢獻：現在完成任何任務時，若玩家已有公會，會額外獲得 公會貢獻度。
   5. 玩家互動指令 (cmd_guild.c)：
       * 新增 guild 指令：查看目前所屬公會、職位與貢獻度。
       * 支援 guild promote：向所屬公會提交晉升申請。

  ✅ 測試結果：
  我建立了 /tests/test_guilds.c，驗證了加入公會、重複加入攔截以及貢獻度達標後的自動晉升邏輯。目前 make test 的結果是
  70/70 全部通過！

  🚀 您可以進行的驗證：
   1. 尋找組織：前往「魔法研究室」或「新兵訓練場」找導師說 加入。
   2. 屬性檢核：如果您屬性不足（例如智力太低），薩魯曼導師會拒絕您的加入申請。
   3. 累積貢獻：加入公會後去打獵狼任務，回報時會看到貢獻度提升。
   4. 申請晉升：貢獻度累積到一定程度（預設 1000 以上），輸入 guild promote 提升職位。

  這個系統為遊戲加入了明確的長期目標與社會階級。

  🛡️  戰鬥團隊與 PK 系統改動摘要：

   1. PK 與紅名機制 (std/living.c)：
       * 新增 pk_score (罪惡值) 與 pk_timer (紅名倒數)。
       * 當玩家惡意殺害無辜者時，罪惡值提升，名字變為 紅名。紅名狀態下所有人皆可合法攻擊。
   2. 實作 PK 守護進程 (secure/pk_d.c)：
       * 合法性判定：自動判斷一次攻擊是否為「惡意」。
       * 團隊保護邏輯：正如您所要求的，如果團隊中的任何成員遭到攻擊，其他所有隊員的反擊都將被判定為「合法自衛」，不會增
         加罪惡值。
   3. 戰利品綁定 (Loot Binding) (std/corpse.c)：
       * 隊伍歸屬：怪物死亡後產生的屍體會記錄擊殺隊伍的 ID。
       * 保護時間：前 30 秒內，只有擊殺者或其隊友可以從屍體中 get 物品。30 秒後保護過期，任何人皆可撿取。
   4. 團隊戰鬥加成 (std/npc.c)：
       * 經驗加成：實作了「人多力量大」獎勵。每增加一名在場的隊員，戰鬥獲得的總經驗值額外提升 10%。
       * 公平分配：獎勵（經驗、金幣、潛能）會自動在在場的隊友間平均分配。
   5. 資訊顯示更新 (cmd_info.c)：
       * score 指令現在會即時顯示玩家的 PK 狀態（普通 / 紅名）。

  ✅ 測試結果：
  我建立了 /tests/test_combat_team.c，模擬了團隊成員被攻擊、隊長合法反擊，以及屍體歸屬綁定的場景。目前 make test
  的結果是 74/74 全部通過！

  🚀 您可以進行的驗證：
   1. 測試團隊自衛：玩家 A 與 B 組隊。讓玩家 C 攻擊 B，此時 A 攻擊 C，輸入 score 查看，A
      的狀態應維持「普通」（合法自衛）。
   2. 測試惡意攻擊：玩家 A 直接攻擊路人 C，若 A 殺死 C，A 將會變為「紅名」。
   3. 測試戰利品：組隊打死一隻怪，讓隊外的人嘗試 get 屍體內的物品，系統應會提示「受保護中」。

  這套系統讓團隊合作更有保障。

  💬 聊天頻道系統摘要：

   1. 分層頻道設計：
       * 全域頻道 (chat)：全服廣播，跨區域溝通。
       * 區域頻道 (area)：僅限同一個地區的玩家收看，增加沈浸感。
       * 公會頻道 (gsay)：所屬組織成員的私密頻道。
       * 團隊頻道 (ttell)：當前隊伍成員的戰術溝通。
       * 房間聊天 (say)：基礎互動，不可關閉，確保當前環境訊息不漏接。
   2. 頻道守護進程 (secure/channel_d.c)：
       * 中央廣播：負責根據頻道類型過濾聽眾（如檢查是否在同隊伍、同公會）。
       * 視覺識別：為不同頻道配置了專屬的顏色前綴（如全域為白色、公會為綠色、團隊為黃色）。
   3. 訂閱管理系統 (std/user.c)：
       * 新增 muted_channels 屬性，玩家可以自主選擇要開啟或關閉哪些頻道。
       * 設定會隨存檔保留，下次登入依然生效。
   4. 玩家互動指令：
       * 發言指令：chat, area, gsay, ttell 加上訊息內容即可發言。
       * 管理指令 channel：
           * channel (不帶參數)：查看目前所有頻道的訂閱狀態。
           * channel <ID> off：關閉指定頻道（例如：channel chat off 暫時安靜一下）。
           * channel <ID> on：重新開啟頻道。

  🚀 您可以進行的驗證：
   1. 測試發言：輸入 chat 大家好，全服都會看到。
   2. 測試屏蔽：輸入 channel chat off，此後您將聽不到任何人的 chat 訊息。
   3. 測試公會發言：加入戰士盟約後，輸入 gsay 為了榮耀，僅限戰友可見。

  這套系統讓 MUD 的社交功能更加完整。

  💰 經濟與頻道系統摘要：                                                                            07:43:31 [10/1876]

   1. 多階貨幣體系 (std/living.c & config.h)：
       * 四級面額：實作了 銅幣、銀幣、金幣 與 酷金幣。
       * 自動換算：兌換比例統一為 1:100。
       * 視覺化顯示：score 指令現在會自動將您的總銅幣餘額轉換為易讀的格式（如：1 酷金幣 50 銀幣）。
   2. 頻道系統升級 (secure/channel_d.c)：
       * 自建好友頻道：玩家現在可以使用 channel <ID> join 建立或加入自定義的私密頻道，並使用 fchat <ID> <訊息>
         進行通訊。
       * 發言收費機制：
           * 公會頻道：每次 1 銀幣。
           * 區域頻道：每次 100 銀幣 (1 金幣)。
           * 全域頻道：每次 10,000 銀幣 (1 酷金幣)。
       * 餘額檢核：系統會在發言前自動扣費，餘額不足將被攔截。
   3. 銀行與利息系統 (secure/bank_d.c)：
       * 存款與提領：玩家可以透過 bank deposit 或 withdraw 來管理財產（帶在身上會掉落，存入銀行才安全）。
       * 停留利息：正如您所要求的，存款會根據玩家的在線時間計算利息。
       * 利息時率：設定為 0.001% /
         小時。每次玩家與銀行互動或進行特定操作時，系統會自動結算這段時間累積的利息並存入帳戶。

  ✅ 測試結果：
  我建立了 /tests/test_currency_chat.c，驗證了貨幣換算、不同頻道的扣費邏輯以及餘額不足時的保護。目前 make test 的結果是
  79/79 全部通過！

  🚀 您可以進行的驗證：
   1. 查看財產：輸入 score 或 bank。
   2. 測試收費：身上帶點銀幣，嘗試在公會發言 (gsay)，會看到扣費提示。
   3. 自建頻道：
       * 輸入 channel 秘密基地 join。
       * 輸入 fchat 秘密基地 有人嗎？。
   4. 銀行操作：輸入 bank deposit 500 存入銅幣。

  這套經濟機制能有效控制頻道洗頻問題，並讓玩家有動力去收集金幣。

  📌 第四階段：寵物與坐騎 (Pets & Mounts)
  相對獨立，但依賴於跟隨系統 (Follow)。
   * 要實作的內容：
       * 捕獲與馴服：將特定 NPC 轉化為寵物。
       * 戰鬥/賣萌：寵物可以加入戰鬥，或有專屬的社交互動指令。
       * 騎乘 (ride)：改變玩家的移動訊息，或許能增加移動速度（若未來有體力設定）。

  🐾 寵物與坐騎系統摘要：

   1. 寵物基礎體系 (std/pet.c)：
       * 忠誠度與歸屬：實作了寵物物件，會自動記錄主人並建立跟隨關係。
       * 戰鬥連動：寵物會自動加入主人的戰鬥，協同攻擊主人的目標。
       * 互動指令：玩家可以使用 pet stay (待命)、pet follow (跟隨) 與 pet dismiss (解散) 來管理寵物。
   2. 馴服機制 (cmd_tame.c)：
       * 技巧性收服：新增 tame 指令。收服成功率受玩家「馴獸術 (taming)」等級與目標「剩餘血量」影響。
       * 策略建議：先將野外生物打至虛弱狀態（血量 < 30%）會大幅提升成功率。
   3. 騎乘系統 (cmd_ride.c)：
       * 身分切換：新增 ride (騎乘) 與 unride (下來) 指令。
       * 視覺整合：騎乘時，玩家的顯示名稱會標註「騎乘中：寵物名」。
       * 同步移動：在 std/living.c 中強化了移動邏輯，確保騎乘中的寵物會與主人完美同步移動。
   4. 野外內容填充：
       * 新生物：在 翠綠草原 (room_6_6) 增加了可馴服的 野馬 (horse.c)。
   5. 核心架構優化 (std/living.c)：
       * 將 leader (領導者) 與 followers (跟隨者) 從玩家專屬移至所有活物的基底類別。這意味著 NPC
         現在也能組隊跟隨，為未來的「護送任務」或「NPC 小隊」打下基礎。

  ✅ 測試結果：
  我建立了 /tests/test_pets_mounts.c，模擬了馴服野馬、進入騎乘狀態以及同步移動的完整流程。目前 make test 的結果是 85/85
  全部通過！

  🚀 您可以進行的驗證：
   1. 尋找目標：前往翠綠草原找到野馬。
   2. 收服寵物：先將馬打到殘血，再輸入 tame horse。
   3. 體驗騎乘：收服後輸入 ride，接著隨意走動，您會看到同步移動的訊息。
   4. 指令管理：輸入 pet stay 讓馬留在原地，走開後再回來輸入 pet follow。

  💀 死亡與跑屍系統摘要：

   1. 嚴厲的死亡懲罰 (std/living.c)：
       * 財產損失：死亡時身上所有金錢會掉落並封裝成「銅幣袋」。
       * 物品掉落：所有非 no_drop (如徽章) 的物品都會掉入屍體。
       * 屬性倒退：
           * 經驗值：損失當前等級升級所需總額的 5%。
           * 潛能：損失當前潛能的 20%。
           * 等級：有 20% 的機率直接下降一個等級。
           * 熟練度：所有已學技能的熟練度 (Exp) 同步下降 5%。
   2. 紅名洗贖機制：
       * 正如您所要求的：死亡會清空 pk_score 與紅名計時。死亡是代價，也是洗清罪孽的方式。
   3. 跑屍恢復機制 (std/corpse.c & cmd_inventory.c)：
       * 靈魂印記：屍體會紀錄死亡時的損失資料與擁有者 ID。
       * 恢復獎勵：如果玩家在屍體消失前（預設 60 秒）回到現場並撿起遺物，系統會判定為「跑屍成功」。
       * 懲罰減半：跑屍成功時，系統會自動返還 50% 損失的經驗值與潛能。
   4. 安全防護優化 (secure/valid.c)：
       * 修正了權限系統，確保玩家始終有權儲存自己的存檔檔案。

  ✅ 測試結果：
  我建立了
  /tests/test_death_corpse.c，模擬了玩家死亡、財產噴發、等級下降、以及事後回到現場取回遺物恢復經驗的完整過程。目前 make
  test 的結果是 91/91 全部通過！

  🚀 您可以進行的驗證：
   1. 測試死亡：讓怪物殺死您，或使用 suicide。
   2. 查看狀態：復活後輸入 score，會發現錢沒了、經驗少了，可能等級也降了。
   3. 執行跑屍：迅速跑回死亡地點，輸入 get all from corpse。您會看到提示：✨ 你取回了自己的遺物，靈魂感到一陣充實...


  💀 死亡與跑屍系統摘要：                                                                            09:14:47 [15/1914]

   1. 嚴厲的死亡懲罰 (std/living.c)：
       * 財產損失：死亡時金錢會噴發成「錢袋」留在屍體內。
       * 屬性倒退：損失升級所需經驗的 5%、潛能的 20%，並有 20% 機率掉一級。
       * 技能衰減：所有已學技能的熟練度同步下降 5%。
   2. 跑屍恢復機制：
       * 靈魂補完：玩家若能在 60 秒內趕回現場撿起遺物，系統將返還 50% 損失的經驗與潛能。
   3. 紅名贖罪：死亡後會自動清空罪惡值與紅名狀態，象徵著生命代價洗清了罪孽。

  🌍 多語系 (i18n) 支援摘要：

   1. 語系守護進程 (secure/language_d.c)：
       * 支援 繁體中文 (zh-TW)、簡體中文 (zh-CN) 與 英文 (en)。
       * 管理所有全域提示訊息與系統反饋。
   2. 自動化語系偵測：
       * Go-LPC 聯動：Driver 現在會自動抓取瀏覽器的 Accept-Language 標頭。
       * 無感切換：新玩家第一次登入時，系統會自動切換至最適合玩家的語言介面。
   3. 開發者友善工具：
       * 在 std/object.c 新增了 _t(key) 輔助函式。未來開發新功能時，只需使用
         _t("welcome")，系統便會根據玩家設定自動翻譯。
   4. 玩家切換功能：
       * 新增 lang <語系> 指令，允許玩家隨時手動更改偏好。


  🌍 多語系支援檢查與強化報告：

   1. 登入首頁 (std/login.c)：
       * 全面本地化：移除了所有硬編碼的中文提示。
       * 自動偵測：現在登入時會自動抓取瀏覽器的 Accept-Language。
       * 語系繼承：新玩家註冊時會自動繼承偵測到的語系，老玩家登入後會載入存檔中的偏好語系。
   2. 指令系統 (cmds/)：
       * 基礎反饋本地化：以 look 指令為範例，現在「找不到目標」或「飄浮在虛空中」等系統訊息會根據玩家語系自動顯示為
         What? 或 什麼？。
       * 標籤本地化：在 std/room.c 中，房間顯示的標題（如：出口、物品、這裡有）已全部接入 _t() 翻譯函式。
   3. 區域地圖 (area/)：
       * 動態描述機制：在 std/room.c 實作了 T_ 前綴偵測。
       * 示範案例：將 中央廣場 (room_4_4) 作為範本，其標題與描述已移至 language_d.c
         字典中。現在不同語系的玩家進入中央廣場，會看到各自語言的詳細描述。
   4. 語言守護進程 (secure/language_d.c)：
       * 擴充了字典庫，包含登入流程、導航標籤、通用錯誤訊息以及首批本地化房間資料。

  🚀 您可以進行的驗證：
   1. 偵測語系：嘗試用英文系統的瀏覽器連線，登入提示應會自動變為英文。
   2. 切換語言：輸入 lang zh-TW 後，當輸入錯誤指令時，提示應變為「什麼？」。
   3. 測試死亡：故意找強怪打死，觀察屬性下降與跑屍恢復的過程。
   5. 觀察變化：
       * 輸入 look：您會看到 Exits、Inventory 等英文標籤。
       * 進入 中央廣場：您會看到英文的 Central Square 描述。
       * 輸入亂碼：系統會回覆 What? 而非 什麼？。

  🏗️  分佈式多語系架構改動摘要：

   1. 基礎類別智慧化 (std/object.c)：
       * 類型相容：set_short 與 set_long 現在同時支援「純字串」與「Mapping 語系對」。
       * 自動選擇器 (select_lang)：所有物件現在具備自動選擇語言的能力。當玩家觀察物件時，系統會自動從 Mapping
         中挑選符合玩家語系的字串（若無則退回英文）。
   2. 精簡語言守護進程 (secure/language_d.c)：
       * 功能定位：移除了所有房間特定描述，僅保留「系統性關鍵字」與「UI 標籤」（例如：Exits、Items、What?）。
       * 高共用性：這確保了字典檔體積精簡，僅用於各個指令間通用的語法反饋。
   3. 房間本地化範例 (room_4_4.c)：
       * 資料回歸：中央廣場的描述已移回房間檔案本身，但以 ([ "en": "...", "zh-TW": "..." ]) 的形式儲存。
       * 自主管理：這讓區域開發者在撰寫房間時，可以同步處理不同語言的內容，而不需要去動系統核心檔案。
   4. UI 與指令優化：
       * std/room.c 的 look_room 現在會自動翻譯「出口：」、「這裡有：」等標籤。
       * process_input (指令未找到) 依然維持多語系反饋。

  ✅ 測試結果：
  我更新了驗證邏輯，確保在分佈式架構下，玩家切換語系後依然能正確看到對應語言。目前 make test 的結果是 95/95 全部通過！

  🚀 您可以進行的驗證：
   1. 進入中央廣場：切換至英文 (lang en) 看到英文描述，切換至繁中 (lang zh-TW) 看到中文描述。
   2. 新開發建議：未來您寫新房間或新 NPC 時，可以直接這樣寫：

   1     set_short(([ "en": "A small dog", "zh-TW": "一隻小狗" ]));

  這套架構兼具了系統的簡潔性與內容的靈活性。

  🗺️  地圖與探索系統 (Map & Fog of War) 摘要：                                                         0:12:57 [22/1860]
   1. 探索迷霧 (Fog of War)：
       * 在玩家物件 (std/user.c) 中實作了 explored_rooms 紀錄。
       * 當玩家移動並進入新房間時，std/room.c 會自動將該房間註記為「已探索」。
   2. 地圖守護進程 (secure/map_d.c)：
       * 實作了座標讀取與動態渲染邏輯。
       * 會根據玩家的探索進度繪製 ASCII 小地圖。未探索的區域會顯示為
         ?，而已探索的區域會根據房間屬性顯示對應的符號（例如：安全區 [S]、鐵匠 [F]、藥劑 [L]、普通房間 [#]）。
   3. 地圖指令 (cmds/cmd_map.c)：
       * 新增 map [範圍] 指令。玩家可以隨時調出 3x3 到 11x11 的周邊地圖，極大地提升了跑圖的便利性。

  🌍 全面多語系化 (Global i18n) 重構摘要：

   1. 分散式字典架構確立：
       * 全面捨棄了臃腫的中央字典。現在，所有的物品、NPC 與房間，都在自己的程式碼中使用 Mapping ([ "en": "...",
         "zh-TW": "..." ]) 來管理多語系描述。
   2. 指令全面本地化 (Commands)：
       * 從基礎的 look, get, drop 到進階的 craft, tame, party, quest，所有的系統回饋訊息皆已替換為動態翻譯。
       * 處理過程中，我同時修復了原本 Go 核心中的一個底層指標崩潰問題，確保系統在缺少語言環境時也能穩定退回預設值。
   3. 世界觀物件本地化 (World Building)：
       * 所有房間：從 room_0_0.c 到 room_7_7.c，所有新手村的場景皆已翻譯為英文與簡體中文。
       * 所有 NPC：包括老闆、導師與野外生物（如：野狼、史萊姆）的名字與描述皆已本地化。
       * 所有物品：包含武器、防具、素材與藥水，現在切換語系後，您的背包內容會瞬間切換語言。

  ✅ 測試結果：
  在經歷了大規模的重構後，我再次執行了包含合成與跑屍在內的完整自動化測試。由於解耦了語言與邏輯的綁定，目前 make test
  的結果是 95/95 完美通過！

  🚀 您可以進行的驗證：
   1. 探索與地圖：
       * 在遊戲中隨意走動幾個房間。
       * 輸入 map 或是 map 3。您會看到漂亮的小地圖，並且只有您走過的房間才會顯示地形符號，周圍則是一片未知的 ?。
   2. 瞬間切換語言：
       * 輸入 lang en 切換為英文。
       * 再次輸入 map，您會發現地圖的圖例與標題已自動變為英文。
       * 輸入 inventory，您身上的物品名稱已變為英文。
       * 輸入 look，房間的描述已自動切換。

  🗺️  Web 虛擬小地圖升級亮點：                                                                                          ]

   1. 後端資料供給 (secure/map_d.c & std/room.c)：
       * 我替 Map Daemon 新增了 get_map_json() 函式，它現在會生成一個 5x5 的二維陣列
         (Grid)，標示每個座標的狀態（玩家、安全區、商店、未探索的迷霧等）。
       * 現在當玩家執行 look 或移動到新房間時，除了原本的純文字描述外，系統還會靜默發送一份 JSON
         格式的小地圖封包給前端。
   2. 前端圖形渲染 (web/static/index.html)：
       * CSS Grid：利用現代瀏覽器的 Grid 佈局，完美重現了 5x5 的矩陣。
       * 色彩編碼 (Color-coded)：
           * 👤 藍色：玩家當前位置
           * 🟩 綠色 (S)：安全區 (如廣場)
           * 🟧 橘色 (F)：鐵匠鋪
           * 🟪 紫色 (L)：藥劑店
           * ⬛ 黑色虛線 (?)：尚未探索的迷霧 (Fog of War)
       * 互動式出口：在玩家周圍相鄰的十字方格內，如果有可通行的出口，會自動轉換為亮綠色的箭頭
         (⬆️ ⬇️ ⬅️ ➡️ )，並且可以直接點擊來進行移動！

  🚀 您可以進行的驗證：
  請重新啟動伺服器 (make run)，並打開瀏覽器進入遊戲：
   1. 看看右方是不是出現了一個精美的 5x5 小地圖！
   2. 嘗試點擊地圖上的 ⬆️  或 ⬇️  箭頭，您的角色應該會自動移動到下一個房間，並且地圖也會即時刷新與解開迷霧。

   * TODO:
   1. 擴充新區域 (地下城與多樓層)：
       * 開始開發 z <> 0 的區域。
       * 例如：洞穴 (蝙蝠)、水域 (水怪)、法師塔。
   2. 將 ASCII 地圖升級為 Web 虛擬地圖：
       * 剛才我們已經在 std/room.c 的 look_room 裡偷塞了 {"ui": "minimap", "data": ...} 的 JSON 封包。
       * 我們可以直接修改 /web/static/index.html，讓網頁端渲染出精美的圖形化小地圖。
   3 氣候
       * 實作時間推進與環境變化。
	   * 日夜：夜行性動物，影響視覺、行動能力
	   * 天氣：霧、雨、雪，最好能讓網頁表達而不是純文字
	   * 四季：不同季節的動物出現與否，影響生物（人、動物）的能力
   4 擴充新區域：利用目前的架構建立地下城 (z < 0)。
