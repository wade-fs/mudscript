disable_wizard(3)     LPC 系統函數     disable_wizard(3)

名稱
     disable_wizard() - 移除物件的巫師設定

語法
     void disable_wizard( void );

描述
     跟 enable_wizard() 函數的作用相反, 用來取消物件的巫師設定.
     注意, 只能用來設定 this_object().

請參考
     enable_wizard(3), wizardp(3)

MudOS                Last change: 5 Sep 1994                    1

