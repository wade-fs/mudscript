deep_inherit_list(3)  LPC 系統函數  deep_inherit_list(3)

名稱
     deep_inherit_list() - 找出物件的所有祖先(遞迴式)

語法
     string *deep_inherit_list( object obj );

描述
     傳回物件 <obj> 的所有祖先的檔名.
     會遞迴式的往前找出所有祖先.

請參考
     inherit_list(3), inherits(3)

MudOS                Last change: 5 Sep 1994                    1

