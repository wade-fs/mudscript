crypt(3)              LPC 系統函數              crypt(3)

名稱
     crypt() - 把一個字串加密保護

語法
     string crypt( string str, string seed );

描述
     把 <str> 以 <seed> 的前兩字元當種子加密保護, 假如沒給 seed,
     則會隨機取兩個字元當種子.
     傳回值的前兩個字元就是種子本身.
     加密方式跟一般的 UNIX 的方式一樣.

MudOS                Last change: 5 Sep 1994                    1

