
BOLD(2)                   silmul_efun                    BOLD(2)

名稱
     bold - 把參數字串變成粗體字

語法
     string bold (string str);

描述
     If  this_player()  has  the  "vt100"  attribute  set,   this
     simul_efun  will  return  str  with  the  "bold  on"  escape
     sequence added in front and the "bold off"  escape  sequence
     added  to the end, so that the string will print out in bold
     mode on a VT100-compatible terminal.

     NOTE:  Do NOT use ed to edit the file vt100.c, or the escape
     sequences  will be corrupted.  (ed strips out escape charac-
     ters).

相關命令
     inverse,  blink,   underscore,   clear_screen,   clear_line,
     up_line, erase_line

作者
     Ichabod@TMI, 3/19/93
     wade@future 4/7/1996 翻譯

MudOS Release 0.9         Last change:                   3-19-93

