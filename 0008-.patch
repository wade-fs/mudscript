From 18b2a6967979f135d292a17cfac07d49ab25b2d7 Mon Sep 17 00:00:00 2001
From: wade <wade.fs@gmail.com>
Date: Mon, 8 Jun 2026 06:12:47 +0800
Subject: [PATCH 08/14] ....

---
 cmd/test-edit/main.go       |  71 -------
 driver/efun_system.go       |  19 ++
 driver/file.go              |  30 +++
 fs/web/static/index.html    | 360 ++++++++++----------------------
 fsmud/web/static/index.html | 402 +++++++++++++-----------------------
 go.mod                      |   2 +-
 6 files changed, 302 insertions(+), 582 deletions(-)
 delete mode 100644 cmd/test-edit/main.go

diff --git a/cmd/test-edit/main.go b/cmd/test-edit/main.go
deleted file mode 100644
index 96aae5dd..00000000
--- a/cmd/test-edit/main.go
+++ /dev/null
@@ -1,71 +0,0 @@
-package main
-
-import (
-	"encoding/json"
-	"fmt"
-	"log"
-	"net/url"
-	"os/exec"
-	"time"
-
-	"github.com/gorilla/websocket"
-)
-
-type WsMsg struct {
-	Type    string `json:"type"`
-	Payload string `json:"payload"`
-}
-
-func main() {
-	cmd := exec.Command("./bin/fsmud", "-mudlib", "fsmud", "-master", "master.c", "-hub", "none", "-port", "8090")
-	if err := cmd.Start(); err != nil { log.Fatalf("Err: %v", err) }
-	defer cmd.Process.Kill()
-	time.Sleep(3 * time.Second)
-
-	u := url.URL{Scheme: "ws", Host: "localhost:8090", Path: "/ws"}
-	conn, _, err := websocket.DefaultDialer.Dial(u.String(), nil)
-	if err != nil { log.Fatalf("WS: %v", err) }
-	defer conn.Close()
-
-	received := make(chan string, 100)
-	go func() {
-		for {
-			_, message, err := conn.ReadMessage()
-			if err != nil { return }
-			var msg WsMsg
-			if err := json.Unmarshal(message, &msg); err == nil {
-				if msg.Type != "" {
-					fmt.Printf("🖥️  [%s]: %s\n", msg.Type, msg.Payload)
-					received <- msg.Type
-				}
-			}
-		}
-	}()
-
-	send := func(text string) {
-		fmt.Printf("⌨️  [Client]: %s\n", text)
-		p, _ := json.Marshal(WsMsg{Type: "cmd", Payload: text})
-		conn.WriteMessage(websocket.TextMessage, p)
-		time.Sleep(1 * time.Second)
-	}
-
-	send("2")      // Traditional Chinese
-	send("wade")   // ID
-	send("jj")     // Pass
-	time.Sleep(3 * time.Second) // Wait for login to finish
-	send("edit /master.c")
-	
-	deadline := time.Now().Add(10 * time.Second)
-	for time.Now().Before(deadline) {
-		select {
-		case t := <-received:
-			if t == "edit_file" {
-				fmt.Println("🎉 SUCCESS: Web IDE edit_file received!")
-				return
-			}
-		default:
-			time.Sleep(100 * time.Millisecond)
-		}
-	}
-	fmt.Println("❌ FAILURE: No edit_file message.")
-}
diff --git a/driver/efun_system.go b/driver/efun_system.go
index 39c7da28..7a23e18e 100644
--- a/driver/efun_system.go
+++ b/driver/efun_system.go
@@ -20,6 +20,24 @@ import (
 // 9. 系統與檔案 (System & Files)
 // ==========================================
 func (d *Driver) registerSystemAndFiles(obj *object.LPCObject) {
+	// 語法: string *get_dir(string path)
+	obj.Vars.Set("get_dir", &object.Builtin{
+		Fn: func(args ...object.Object) object.Object {
+			if len(args) < 1 { return &object.Array{Elements: []object.Object{}} }
+			path, ok := args[0].(*object.String)
+			if !ok { return &object.Array{Elements: []object.Object{}} }
+
+			files, err := d.GetDir(path.Value)
+			if err != nil { return &object.Array{Elements: []object.Object{}} }
+
+			elements := make([]object.Object, len(files))
+			for i, f := range files {
+				elements[i] = &object.String{Value: f}
+			}
+			return &object.Array{Elements: elements}
+		},
+	})
+
 	// 語法: int rm(string file)
 	// 說明: 刪除檔案。回傳 1 成功，0 失敗。
 	// 範例: rm("/log/old.log");
@@ -872,3 +890,4 @@ func (d *Driver) registerMonitorEfuns(obj *object.LPCObject) {
 		},
 	})
 }
+
diff --git a/driver/file.go b/driver/file.go
index 4763cf33..a62f742e 100644
--- a/driver/file.go
+++ b/driver/file.go
@@ -136,3 +136,33 @@ func (d *Driver) formatParserErrors(filename string, errors []string) error {
 	}
 	return fmt.Errorf("%s", sb.String())
 }
+
+// 🚀 新增：掃描目錄檔案 (get_dir)
+func (d *Driver) GetDir(path string) ([]string, error) {
+	resolvedPath := d.ResolvePath("", path)
+	
+	// 處理萬用字元 (如 /data/*.o)
+	if strings.Contains(resolvedPath, "*") {
+		fullPattern := filepath.Join(d.Config.MudLibPath, resolvedPath)
+		matches, err := filepath.Glob(fullPattern)
+		if err != nil { return nil, err }
+		
+		var results []string
+		for _, m := range matches {
+			results = append(results, filepath.Base(m))
+		}
+		return results, nil
+	}
+
+	fullPath := filepath.Join(d.Config.MudLibPath, resolvedPath)
+	files, err := os.ReadDir(fullPath)
+	if err != nil { return nil, err }
+
+	var results []string
+	for _, f := range files {
+		name := f.Name()
+		if f.IsDir() { name += "/" }
+		results = append(results, name)
+	}
+	return results, nil
+}
diff --git a/fs/web/static/index.html b/fs/web/static/index.html
index a5019fe0..81c6da8d 100644
--- a/fs/web/static/index.html
+++ b/fs/web/static/index.html
@@ -76,7 +76,7 @@
         .suggest-item { padding: 10px 20px; cursor: pointer; border-bottom: 1px solid #333; display: flex; justify-content: space-between; }
         .suggest-item.active { background: #4CAF50; color: #fff; }
     </style>
-<script src="https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs/loader.js"></script>
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs/loader.js"></script>
 </head>
 <body>
 <div id="wrapper">
@@ -96,13 +96,17 @@
         <div id="cmds-area" class="panel-content">Loading commands...</div>
     </div>
 
-    
-    <div id="panel-editor" class="overlay-panel" style="width: 80%; height: 80%; top: 10%; left: 10%; border: 2px solid #2196F3;">
+    <div id="panel-socials" class="overlay-panel">
+        <div class="panel-header">Social Actions <span onclick="togglePanel('panel-socials')" style="cursor:pointer">×</span></div>
+        <div id="socials-area" class="panel-content"></div>
+    </div>
+
+    <div id="panel-editor" class="overlay-panel" style="width: 80%; height: 80%; top: 10%; left: 10%; border: 2px solid #2196F3; z-index: 2000;">
         <div class="panel-header" style="background: #1565C0;">
             <span id="editor-title">Editor</span>
             <span onclick="togglePanel('panel-editor')" style="cursor:pointer">×</span>
         </div>
-        <div class="panel-content" style="display:flex; flex-direction:column; gap:10px; padding: 0;">
+        <div class="panel-content" style="display:flex; flex-direction:column; gap:0; padding: 0;">
             <div id="editor-container" style="flex:1; background:#1e1e1e;"></div>
             <div style="background:#252526; padding:10px 15px; display:flex; gap:10px; border-top:1px solid #333;">
                 <button class="tool-btn" onclick="saveEditorFile()" style="background:#2196F3; border-color:#2196F3;">💾 Save (Ctrl+S)</button>
@@ -111,10 +115,6 @@
             </div>
         </div>
     </div>
-    <div id="panel-socials" class="overlay-panel">
-        <div class="panel-header">Social Actions <span onclick="togglePanel('panel-socials')" style="cursor:pointer">×</span></div>
-        <div id="socials-area" class="panel-content"></div>
-    </div>
 
     <div id="panel-import" class="overlay-panel">
         <div class="panel-header">Import Emoji Map <span onclick="togglePanel('panel-import')" style="cursor:pointer">×</span></div>
@@ -181,6 +181,8 @@
 <script>
     let ws, historyCmds = [], histIdx = -1, msgInput, suggestIdx = -1, isInputHidden = false;
     let mcData = null, mcMode = false, mcAction = 'dig', mcZoom = 32;
+    let monacoEditor = null, currentEditPath = "";
+
     const mcColors = {
         grass:'#4CAF50', dirt:'#795548', stone:'#9E9E9E',
         wood:'#8D6E63', log:'#5D4037', planks:'#FFCC80',
@@ -189,6 +191,39 @@
         brick:'#BF360C', player:'#2196F3', self:'#FFEB3B'
     };
 
+    // Monaco 初始化 (安全檢查)
+    if (typeof require !== "undefined") {
+        require.config({ paths: { 'vs': 'https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs' }});
+        require(['vs/editor/editor.main'], function() {
+            monaco.languages.register({ id: 'lpc' });
+            monaco.languages.setMonarchTokensProvider('lpc', {
+                tokenizer: {
+                    root: [
+                        [/[a-zA-Z_]\w*/, {
+                            cases: {
+                                'inherit|mixed|object|string|int|float|mapping|array|void|private|protected|public|nomask|varargs|virtual|static|status': 'keyword',
+                                '@default': 'identifier'
+                            }
+                        }],
+                        { include: 'vs/basic-languages/cpp/cpp' }
+                    ]
+                }
+            });
+
+            monacoEditor = monaco.editor.create(document.getElementById('editor-container'), {
+                value: '',
+                theme: 'vs-dark',
+                automaticLayout: true,
+                fontSize: 16,
+                minimap: { enabled: true }
+            });
+
+            monacoEditor.addCommand(monaco.KeyMod.CtrlCmd | monaco.KeyCode.KeyS, function() {
+                saveEditorFile();
+            });
+        });
+    }
+
     window.onload = () => {
         msgInput = document.getElementById("msg");
         initLocalization();
@@ -200,11 +235,8 @@
     function togglePanel(id, btn) {
         const panel = document.getElementById(id);
         const isActive = panel.classList.contains('active');
-        
-        // 關閉所有面板
         document.querySelectorAll('.overlay-panel').forEach(p => p.classList.remove('active'));
         document.querySelectorAll('.tool-btn').forEach(b => b.classList.remove('active'));
-
         if (!isActive) {
             panel.classList.add('active');
             if (btn) btn.classList.add('active');
@@ -228,8 +260,9 @@
         ws = new WebSocket(protocol + "//" + location.host + "/ws");
         ws.onmessage = (e) => {
             const msg = JSON.parse(e.data);
-            if (msg.type === "mud_text" || msg.type === "mud_html" || msg.type === "edit_file" || msg.type === "chat") {
-                const p = (msg.type === "chat" ? (msg.username + ": " + msg.payload) : msg.payload).trim();
+            // 🚀 核心優化：處理任何包含 payload 的訊息
+            if (msg.payload !== undefined && msg.payload !== null) {
+                const p = (msg.type === "chat" ? (msg.username + ": " + msg.payload) : msg.payload);
                 if (p === "__INPUT_HIDDEN__") {
                     isInputHidden = true;
                     msgInput.style.webkitTextSecurity = "disc";
@@ -239,7 +272,7 @@
                 if (msg.type === "mud_html") { appendRaw(p); return; }
                 if (msg.type === "edit_file") { openEditor(JSON.parse(p)); return; }
 
-                if (p.startsWith("{") && p.endsWith("}")) {
+                if (typeof p === "string" && p.startsWith("{") && p.endsWith("}")) {
                     try { handleUIData(JSON.parse(p)); } catch (err) { handleMultiJSON(p); }
                 } else {
                     appendLook(p);
@@ -261,7 +294,7 @@
             document.getElementById("info-area").innerHTML = `
                 <div class="stat-box"><span class="stat-label">❤️ HP</span><span class="stat-value">${d.hp}/${d.max_hp}</span></div>
                 <div class="stat-box"><span class="stat-label">✨ MP</span><span class="stat-value">${d.mp}/${d.max_mp}</span></div>
-                <div class="stat-box"><span class="stat-label">💰 GOLD</span><span class="stat-value">${parseColorTags(d.money)}</span></div>
+                <div class="stat-box"><span class="stat-label">💰 GOLD</span><span class="stat-value">${parseOutput(d.money)}</span></div>
             `;
         } else if (ui.ui === "cmd_list") { renderCmdList(ui.data, ui.title); window.allCommands = ui.data; }
         else if (ui.ui === "socials") { renderSocials(ui.data, ui.title); window.allSocials = ui.data; }
@@ -271,6 +304,31 @@
         else if (ui.ui === "mc_exit") toggleMCMode(false); 
     }
 
+    function openEditor(data) {
+        currentEditPath = data.path;
+        document.getElementById("editor-title").innerText = "📝 Editing: " + data.path;
+        if (monacoEditor) {
+            const ext = data.path.split('.').pop();
+            const lang = (ext === 'c' || ext === 'h') ? 'cpp' : 'plaintext';
+            monaco.editor.setModelLanguage(monacoEditor.getModel(), lang);
+            monacoEditor.setValue(data.content);
+        }
+        document.getElementById("editor-status").innerText = "";
+        togglePanel('panel-editor');
+        if (monacoEditor) setTimeout(() => monacoEditor.focus(), 100);
+    }
+
+    function saveEditorFile() {
+        if (!currentEditPath || !monacoEditor) return;
+        const content = monacoEditor.getValue();
+        ws.send(JSON.stringify({ 
+            type: "save_file", 
+            payload: JSON.stringify({ path: currentEditPath, content: content }) 
+        }));
+        document.getElementById("editor-status").innerText = "Saving...";
+        setTimeout(() => { document.getElementById("editor-status").innerText = "Saved at " + new Date().toLocaleTimeString(); }, 500);
+    }
+
     function renderCmdList(cats, title) {
         const area = document.getElementById("cmds-area");
         area.innerHTML = "";
@@ -302,9 +360,7 @@
     function handleMCMap(data) {
         if (!mcMode) toggleMCMode(true);
         mcData = data;
-        if (data.world_name) {
-            document.querySelector("#mc-ui span").innerText = data.world_name;
-        }
+        if (data.world_name) document.querySelector("#mc-ui span").innerText = data.world_name;
         renderMCMap();
     }
 
@@ -349,9 +405,7 @@
         const selfPos = players[self_id] || [Math.floor(width/2), Math.floor(height/2)];
         const cw = canvas.width, ch = canvas.height;
         const centerX = cw / 2, centerY = ch / 2;
-
         ctx.fillStyle = '#0d1b2a'; ctx.fillRect(0, 0, cw, ch);
-
         const wx0 = centerX + (0 - selfPos[0]) * mcZoom;
         const wy0 = centerY - (0 - selfPos[1]) * mcZoom;
         const wx1 = centerX + (width - selfPos[0]) * mcZoom;
@@ -360,7 +414,6 @@
         ctx.setLineDash([6, 4]);
         ctx.strokeRect(wx0, wy1, wx1 - wx0, wy0 - wy1);
         ctx.setLineDash([]);
-
         if (mcZoom >= 20) {
             ctx.strokeStyle = 'rgba(255,255,255,0.04)'; ctx.lineWidth = 0.5;
             const startX = Math.floor((0 - centerX / mcZoom) + selfPos[0]);
@@ -374,7 +427,6 @@
                 ctx.beginPath(); ctx.moveTo(0, sy); ctx.lineTo(cw, sy); ctx.stroke();
             }
         }
-
         for (let key in blocks) {
             const [bx, by] = key.split(',').map(Number);
             const sx = centerX + (bx - selfPos[0]) * mcZoom;
@@ -386,14 +438,7 @@
                 ctx.strokeStyle = 'rgba(0,0,0,0.25)'; ctx.lineWidth = 0.5;
                 ctx.strokeRect(sx, sy - mcZoom, mcZoom, mcZoom);
             }
-            if (mcZoom >= 40) {
-                ctx.fillStyle = 'rgba(255,255,255,0.7)';
-                ctx.font = Math.max(8, mcZoom/4) + 'px monospace';
-                ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
-                ctx.fillText(blocks[key], sx + mcZoom/2, sy - mcZoom/2);
-            }
         }
-
         for (let pid in players) {
             const ppos = players[pid];
             const sx = centerX + (ppos[0] - selfPos[0]) * mcZoom;
@@ -409,28 +454,12 @@
             ctx.textAlign = 'center'; ctx.textBaseline = 'bottom';
             ctx.fillText(pid, sx + mcZoom/2, sy - mcZoom - 2);
         }
-
-        if (mcData.npcs) {
-            for (let nid in mcData.npcs) {
-                const npos = mcData.npcs[nid];
-                const sx = centerX + (npos[0] - selfPos[0]) * mcZoom;
-                const sy = centerY - (npos[1] - selfPos[1]) * mcZoom;
-                const icon = npos[2] || '👾';
-                ctx.font = Math.max(12, mcZoom * 0.8) + 'px sans-serif';
-                ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
-                ctx.fillText(icon, sx + mcZoom/2, sy - mcZoom/2);
-            }
-        }
-
         ctx.fillStyle = 'rgba(0,0,0,0.55)'; ctx.fillRect(6, 6, 160, 22);
         ctx.fillStyle = '#adf'; ctx.font = '12px monospace'; ctx.textAlign = 'left'; ctx.textBaseline = 'top';
         ctx.fillText(`pos (${selfPos[0]}, ${selfPos[1]})  zoom ${mcZoom}px`, 10, 10);
     }
 
-    function promptImportMap() {
-        togglePanel('panel-import');
-    }
-
+    function promptImportMap() { togglePanel('panel-import'); }
     function submitMapImport() {
         const input = document.getElementById("import-area").value;
         if (input) {
@@ -454,101 +483,6 @@
         m.innerHTML = html; m.style.display = "grid";
     }
 
-    
-    let currentEditPath = "";
-    function openEditor(data) {
-        currentEditPath = data.path;
-        document.getElementById("editor-title").innerText = "📝 Editing: " + data.path;
-        document.getElementById("editor-area").value = data.content;
-        document.getElementById("editor-status").innerText = "";
-        togglePanel('panel-editor');
-        setTimeout(() => document.getElementById("editor-area").focus(), 100);
-    }
-
-    function saveEditorFile() {
-        if (!currentEditPath) return;
-        const content = document.getElementById("editor-area").value;
-        ws.send(JSON.stringify({ type: "cmd", payload: "__save_web_file__ " + currentEditPath + "\n" + content }));
-        document.getElementById("editor-status").innerText = "Saving...";
-        setTimeout(() => { document.getElementById("editor-status").innerText = "Saved at " + new Date().toLocaleTimeString(); }, 500);
-    }
-
-    // 監聽 Ctrl+S
-    document.addEventListener('keydown', (e) => {
-        if (e.ctrlKey && e.key === 's') {
-            const editor = document.getElementById('panel-editor');
-            if (editor.classList.contains('active')) {
-                e.preventDefault();
-                saveEditorFile();
-            }
-        }
-    });
-
-    
-    let monacoEditor = null;
-    let currentEditPath = "";
-
-    // 初始化 Monaco
-    require.config({ paths: { 'vs': 'https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs' }});
-    require(['vs/editor/editor.main'], function() {
-        // 定義 LPC 語法高亮 (基於 C 擴充)
-        monaco.languages.register({ id: 'lpc' });
-        monaco.languages.setMonarchTokensProvider('lpc', {
-            tokenizer: {
-                root: [
-                    [/[a-zA-Z_]\w*/, {
-                        cases: {
-                            'inherit|mixed|object|string|int|float|mapping|array|void|private|protected|public|nomask|varargs|virtual|static|status': 'keyword',
-                            '@default': 'identifier'
-                        }
-                    }],
-                    { include: 'vs/basic-languages/cpp/cpp' } // 繼承 C++ 基礎
-                ]
-            }
-        });
-
-        monacoEditor = monaco.editor.create(document.getElementById('editor-container'), {
-            value: '',
-            language: 'cpp', // 暫時先用 cpp，之後改進 lpc
-            theme: 'vs-dark',
-            automaticLayout: true,
-            fontSize: 16,
-            minimap: { enabled: true }
-        });
-
-        // 快捷鍵支援
-        monacoEditor.addCommand(monaco.KeyMod.CtrlCmd | monaco.KeyCode.KeyS, function() {
-            saveEditorFile();
-        });
-    });
-
-    function openEditor(data) {
-        currentEditPath = data.path;
-        document.getElementById("editor-title").innerText = "📝 Editing: " + data.path;
-        
-        if (monacoEditor) {
-            const ext = data.path.split('.').pop();
-            const lang = (ext === 'c' || ext === 'h') ? 'cpp' : 'plaintext';
-            monaco.editor.setModelLanguage(monacoEditor.getModel(), lang);
-            monacoEditor.setValue(data.content);
-        }
-        
-        document.getElementById("editor-status").innerText = "";
-        togglePanel('panel-editor');
-        if (monacoEditor) setTimeout(() => monacoEditor.focus(), 100);
-    }
-
-    function saveEditorFile() {
-        if (!currentEditPath || !monacoEditor) return;
-        const content = monacoEditor.getValue();
-        ws.send(JSON.stringify({ 
-            type: "save_file", 
-            payload: JSON.stringify({ path: currentEditPath, content: content }) 
-        }));
-        document.getElementById("editor-status").innerText = "Saving...";
-        setTimeout(() => { document.getElementById("editor-status").innerText = "Saved at " + new Date().toLocaleTimeString(); }, 500);
-    }
-
     function sendCmd(cmd) {
         if (cmd !== "") {
             if (historyCmds.length === 0 || historyCmds[historyCmds.length - 1] !== cmd) {
@@ -562,7 +496,6 @@
         appendRaw("<span style='color:#888'>&gt;&gt; " + echo + "</span>");
     }
 
-    // appendRaw: 已是 HTML，直接插入，不做 escape 或 parseOutput
     function appendRaw(html) {
         const area = document.getElementById("look-area"), content = document.getElementById("look-content");
         const div = document.createElement("div");
@@ -573,95 +506,64 @@
     }
 
     function appendLook(t) {
+        if (typeof t !== "string") return;
         const area = document.getElementById("look-area"), content = document.getElementById("look-content");
-        // 所有輸入一律視為純文字，先 escape HTML，再交給 parseOutput 處理
-        let p = t
-            .replace(/&/g, "&amp;")
-            .replace(/</g, "&lt;")
-            .replace(/>/g, "&gt;");
+        let p = t.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
         p = parseOutput(p);
-        // [顯示文字|指令] 可點擊連結
         p = p.replace(/\[(.*?)\]/g, (m, p1) => {
             let pts = p1.split('|'), d = pts[0], c = pts.length > 1 ? pts[1] : pts[0];
             return `<span class="cmd-link" onclick="sendCmd('${c.replace(/'/g, "\\'")}')">${d}</span>`;
         });
-        const div = document.createElement("div");
-        div.style.whiteSpace = "pre-wrap";
-        div.innerHTML = p;
-        content.appendChild(div);
-        setTimeout(() => { area.scrollTop = area.scrollHeight; }, 10);
+        appendRaw(p);
     }
 
-    // parseOutput: 統一處理所有輸出轉換
-    // 順序：ANSI → $TAG$ → {tag} → markdown
-    // 輸入已是 HTML-escaped 純文字，輸出是帶 HTML 標籤的字串。
     function parseOutput(t) {
-        let res = t;
-        let open = 0;
-        // ── 0. 標題佔位符（最先跑，在所有 HTML 生成之前）────────────────────
+        if (typeof t !== "string") return t;
+        let res = t, open = 0;
         res = res.replace(/###([^#\n]+)###/g, "\x00H3\x00$1\x00/H3\x00");
         res = res.replace(/##([^#\n]+)##/g,   "\x00H2\x00$1\x00/H2\x00");
         res = res.replace(/#([^#\n]+)#/g,     "\x00H1\x00$1\x00/H1\x00");
 
-
-        // ── 1. ANSI escape code ─────────────────────────────────────────────
-        // 串連式用法：HIR + "文字" + NOR 展開後是 \x1b[1;31m文字\x1b[0m
         const ansiMap = {
-            "0;30":"#555",    "0;31":"#ff5555", "0;32":"#55ff55", "0;33":"#ffff55",
-            "0;34":"#5555ff", "0;35":"#ff55ff", "0;36":"#55ffff", "0;37":"#ffffff",
-            "30":"#555",      "31":"#ff5555",   "32":"#55ff55",   "33":"#ffff55",
-            "34":"#5555ff",   "35":"#ff55ff",   "36":"#55ffff",   "37":"#ffffff",
-            "1;30":"#888",    "1;31":"#ff7777", "1;32":"#77ff77", "1;33":"#ffff77",
-            "1;34":"#7777ff", "1;35":"#ff77ff", "1;36":"#77ffff", "1;37":"#ffffff",
-            "41;1":"bg:#a33", "42;1":"bg:#3a3", "43;1":"bg:#aa3",
-            "44;1":"bg:#33a", "45;1":"bg:#a3a", "46;1":"bg:#3aa", "47;1":"bg:#aaa",
-            "40":"bg:#000",   "41":"bg:#500",   "42":"bg:#050",   "43":"bg:#550",
-            "44":"bg:#005",   "45":"bg:#505",   "46":"bg:#055",   "47":"bg:#555",
+            "0;30":"#555", "0;31":"#ff5555", "0;32":"#55ff55", "0;33":"#ffff55", "0;34":"#5555ff", "0;35":"#ff55ff", "0;36":"#55ffff", "0;37":"#ffffff",
+            "30":"#555", "31":"#ff5555", "32":"#55ff55", "33":"#ffff55", "34":"#5555ff", "35":"#ff55ff", "36":"#55ffff", "37":"#ffffff",
+            "1;30":"#888", "1;31":"#ff7777", "1;32":"#77ff77", "1;33":"#ffff77", "1;34":"#7777ff", "1;35":"#ff77ff", "1;36":"#77ffff", "1;37":"#ffffff",
+            "41;1":"bg:#a33", "42;1":"bg:#3a3", "43;1":"bg:#aa3", "44;1":"bg:#33a", "45;1":"bg:#a3a", "46;1":"bg:#3aa", "47;1":"bg:#aaa",
+            "40":"bg:#000", "41":"bg:#500", "42":"bg:#050", "43":"bg:#550", "44":"bg:#005", "45":"bg:#505", "46":"bg:#055", "47":"bg:#555",
             "1":"bold", "4":"underline", "5":"blink", "7":"reverse", "1;7":"bold-reverse",
         };
         res = res.replace(/\x1b\[([0-9;]+)m/g, (m, code) => {
             if (code === "0" || code === "2;37;0") {
                 let s = ""; while (open > 0) { s += "</span>"; open--; } return s;
             }
-            const v = ansiMap[code];
-            if (!v) return "";
+            const v = ansiMap[code]; if (!v) return "";
             open++;
-            if (v === "bold")         return `<span style="font-weight:bold">`;
-            if (v === "underline")    return `<span style="text-decoration:underline">`;
-            if (v === "blink")        return `<span style="animation:blink 1s step-end infinite">`;
-            if (v === "reverse")      return `<span style="filter:invert(1)">`;
+            if (v === "bold") return `<span style="font-weight:bold">`;
+            if (v === "underline") return `<span style="text-decoration:underline">`;
+            if (v === "blink") return `<span style="animation:blink 1s step-end infinite">`;
+            if (v === "reverse") return `<span style="filter:invert(1)">`;
             if (v === "bold-reverse") return `<span style="font-weight:bold;filter:invert(1)">`;
-            if (v.startsWith("bg:"))  return `<span style="background:${v.slice(3)}">`;
+            if (v.startsWith("bg:")) return `<span style="background:${v.slice(3)}">`;
             return `<span style="color:${v}">`;
         });
         while (open > 0) { res += "</span>"; open--; }
         open = 0;
 
-        // ── 2. $TAG$ 嵌入式顏色巨集 ─────────────────────────────────────────
-        // 嵌入式用法："xxx$HIY$yyy$NOR$zzz"
-        // $ 不是 HTML 特殊字元，escape 後仍是原樣
         if (res.includes("$")) {
             const tagMap = {
-                "$BLK$":"#555",     "$RED$":"#ff5555", "$GRN$":"#55ff55", "$YEL$":"#ffff55",
-                "$BLU$":"#5555ff",  "$MAG$":"#ff55ff", "$CYN$":"#55ffff", "$WHT$":"#ffffff",
-                "$HIK$":"#888",
-                "$HIR$":"#ff7777",  "$HIG$":"#77ff77", "$HIY$":"#ffff77",
-                "$HIB$":"#7777ff",  "$HIM$":"#ff77ff", "$HIC$":"#77ffff", "$HIW$":"#ffffff",
-                "$U$":"underline",  "$REV$":"reverse", "$HIREV$":"bold-reverse",
-                "$BLINK$":"blink",  "$BOLD$":"bold",
+                "$BLK$":"#555", "$RED$":"#ff5555", "$GRN$":"#55ff55", "$YEL$":"#ffff55", "$BLU$":"#5555ff", "$MAG$":"#ff55ff", "$CYN$":"#55ffff", "$WHT$":"#ffffff",
+                "$HIK$":"#888", "$HIR$":"#ff7777", "$HIG$":"#77ff77", "$HIY$":"#ffff77", "$HIB$":"#7777ff", "$HIM$":"#ff77ff", "$HIC$":"#77ffff", "$HIW$":"#ffffff",
+                "$U$":"underline", "$REV$":"reverse", "$HIREV$":"bold-reverse", "$BLINK$":"blink", "$BOLD$":"bold",
             };
             res = res.replace(/\$([A-Z0-9]+)\$/g, (m, name) => {
                 const key = "$" + name + "$";
-                if (key === "$NOR$") {
-                    let s = ""; while (open > 0) { s += "</span>"; open--; } return s;
-                }
-                const v = tagMap[key];
-                if (!v) return m;
+                if (key === "$NOR$") { let s = ""; while (open > 0) { s += "</span>"; open--; } return s; }
+                const v = tagMap[key]; if (!v) return m;
                 open++;
-                if (v === "bold")         return `<span style="font-weight:bold">`;
-                if (v === "underline")    return `<span style="text-decoration:underline">`;
-                if (v === "blink")        return `<span style="animation:blink 1s step-end infinite">`;
-                if (v === "reverse")      return `<span style="filter:invert(1)">`;
+                if (v === "bold") return `<span style="font-weight:bold">`;
+                if (v === "underline") return `<span style="text-decoration:underline">`;
+                if (v === "blink") return `<span style="animation:blink 1s step-end infinite">`;
+                if (v === "reverse") return `<span style="filter:invert(1)">`;
                 if (v === "bold-reverse") return `<span style="font-weight:bold;filter:invert(1)">`;
                 return `<span style="color:${v}">`;
             });
@@ -669,48 +571,25 @@
             open = 0;
         }
 
-        // ── 3. {r}{g} 舊式標籤（向下相容）──────────────────────────────────
         if (res.includes("{")) {
-            const oldMap = {
-                "r":"#ff5555","g":"#55ff55","b":"#5555ff","y":"#ffff55",
-                "c":"#55ffff","m":"#ff55ff","w":"#ffffff","gr":"#888",
-            };
+            const oldMap = { "r":"#ff5555","g":"#55ff55","b":"#5555ff","y":"#ffff55","c":"#55ffff","m":"#ff55ff","w":"#ffffff","gr":"#888" };
             res = res.replace(/\{([a-z]+)\}/g, (m, tag) => {
-                if (tag === "n") {
-                    let s = ""; while (open > 0) { s += "</span>"; open--; } return s;
-                }
-                if (oldMap[tag]) { open++; return `<span style="color:${oldMap[tag]}">`;  }
+                if (tag === "n") { let s = ""; while (open > 0) { s += "</span>"; open--; } return s; }
+                if (oldMap[tag]) { open++; return `<span style="color:${oldMap[tag]}">`; }
                 return m;
             });
             while (open > 0) { res += "</span>"; open--; }
         }
 
-        // ── 4. Markdown-like 語法（標題已在步驟 0 標記）───────────────────────
-        // 分隔線（單獨一行）
         res = res.replace(/^---\s*$/gm, "<hr style='border:none;border-top:1px solid #444;margin:4px 0'>");
         res = res.replace(/^===\s*$/gm, "<hr style='border:none;border-top:2px double #888;margin:4px 0'>");
-
-        // 刪除線
-        res = res.replace(/~~([^\n~]+)~~/g,
-            "<span style='text-decoration:line-through;opacity:0.7'>$1</span>");
-
-        // 粗斜體 *** 先比對
-        res = res.replace(/\*{3}([^\n*]+)\*{3}/g,
-            "<span style='font-weight:bold;font-style:italic;color:#ffe'>$1</span>");
-        // 粗體
-        res = res.replace(/\*{2}([^\n*]+)\*{2}/g,
-            "<span style='font-weight:bold;color:#fff'>$1</span>");
-        // 斜體
-        res = res.replace(/\*([^\n*]+)\*/g,
-            "<span style='font-style:italic;color:#cce'>$1</span>");
-
-        // 佔位符 → 標題 HTML（inline span，不換行）
-        res = res.replace(/\x00H3\x00([^\x00]*)\x00\/H3\x00/g,
-            "<span style='font-size:1.0em;font-weight:bold;color:rgb(170,170,170)'>$1</span>");
-        res = res.replace(/\x00H2\x00([^\x00]*)\x00\/H2\x00/g,
-            "<span style='font-size:1.2em;font-weight:bold;color:rgb(221,221,221)'>$1</span>");
-        res = res.replace(/\x00H1\x00([^\x00]*)\x00\/H1\x00/g,
-            "<span style='font-size:1.4em;font-weight:bold;color:rgb(255,255,119)'>$1</span>");
+        res = res.replace(/~~([^\n~]+)~~/g, "<span style='text-decoration:line-through;opacity:0.7'>$1</span>");
+        res = res.replace(/\*{3}([^\n*]+)\*{3}/g, "<span style='font-weight:bold;font-style:italic;color:#ffe'>$1</span>");
+        res = res.replace(/\*{2}([^\n*]+)\*{2}/g, "<span style='font-weight:bold;color:#fff'>$1</span>");
+        res = res.replace(/\*([^\n*]+)\*/g, "<span style='font-style:italic;color:#cce'>$1</span>");
+        res = res.replace(/\x00H3\x00([^\x00]*)\x00\/H3\x00/g, "<span style='font-size:1.0em;font-weight:bold;color:rgb(170,170,170)'>$1</span>");
+        res = res.replace(/\x00H2\x00([^\x00]*)\x00\/H2\x00/g, "<span style='font-size:1.2em;font-weight:bold;color:rgb(221,221,221)'>$1</span>");
+        res = res.replace(/\x00H1\x00([^\x00]*)\x00\/H1\x00/g, "<span style='font-size:1.4em;font-weight:bold;color:rgb(255,255,119)'>$1</span>");
 
         return res;
     }
@@ -726,11 +605,7 @@
     }
 
     function initInputHandlers() {
-        msgInput.onclick = (e) => {
-            e.stopPropagation();
-            if (historyCmds.length > 0) showHistory();
-        };
-
+        msgInput.onclick = (e) => { e.stopPropagation(); if (historyCmds.length > 0) showHistory(); };
         window.addEventListener('keydown', (e) => {
             if (mcMode) {
                 if (document.activeElement === msgInput && msgInput.value !== "" && e.key !== "Enter") return;
@@ -738,7 +613,6 @@
                 let dir = { w: 'n', s: 's', a: 'w', d: 'e', arrowup: 'n', arrowdown: 's', arrowleft: 'w', arrowright: 'e' }[key];
                 if (dir) { e.preventDefault(); sendCmd(`mc move ${dir}`); return; }
             }
-
             if (document.activeElement === msgInput) {
                 if (e.key === "ArrowUp") { e.preventDefault(); if (histIdx > 0) { histIdx--; msgInput.value = historyCmds[histIdx]; } }
                 else if (e.key === "ArrowDown") { e.preventDefault(); if (histIdx < historyCmds.length - 1) { histIdx++; msgInput.value = historyCmds[histIdx]; } else { histIdx = historyCmds.length; msgInput.value = ""; } }
@@ -752,9 +626,7 @@
         const box = document.getElementById("history-box");
         let html = '<div class="history-header"><span>Command History</span><span onclick="hideHistory()" style="cursor:pointer">×</span></div>';
         const recent = historyCmds.slice(-10).reverse();
-        recent.forEach(cmd => {
-            html += `<div class="history-item" onclick="useHistory('${cmd.replace(/'/g, "\\'")}')">${cmd}</div>`;
-        });
+        recent.forEach(cmd => { html += `<div class="history-item" onclick="useHistory('${cmd.replace(/'/g, "\\'")}')">${cmd}</div>`; });
         box.innerHTML = html; box.style.display = "block";
     }
 
@@ -762,12 +634,8 @@
     function useHistory(cmd) { msgInput.value = cmd; hideHistory(); msgInput.focus(); }
 
     function sendMsg() { 
-        const v = msgInput.value;
-        sendCmd(v); 
-        msgInput.value = ""; 
-        isInputHidden = false;
-        msgInput.style.webkitTextSecurity = "none";
-        msgInput.focus(); 
+        const v = msgInput.value; sendCmd(v); msgInput.value = ""; 
+        isInputHidden = false; msgInput.style.webkitTextSecurity = "none"; msgInput.focus(); 
     }
     function fillInput(c) { msgInput.value = c + " "; msgInput.focus(); }
     function handleNatureData(d) {
diff --git a/fsmud/web/static/index.html b/fsmud/web/static/index.html
index e157e71f..03c7ab79 100644
--- a/fsmud/web/static/index.html
+++ b/fsmud/web/static/index.html
@@ -76,7 +76,7 @@
         .suggest-item { padding: 10px 20px; cursor: pointer; border-bottom: 1px solid #333; display: flex; justify-content: space-between; }
         .suggest-item.active { background: #4CAF50; color: #fff; }
     </style>
-<script src="https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs/loader.js"></script>
+    <script src="https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs/loader.js"></script>
 </head>
 <body>
 <div id="wrapper">
@@ -96,13 +96,17 @@
         <div id="cmds-area" class="panel-content">Loading commands...</div>
     </div>
 
-    
-    <div id="panel-editor" class="overlay-panel" style="width: 80%; height: 80%; top: 10%; left: 10%; border: 2px solid #2196F3;">
+    <div id="panel-socials" class="overlay-panel">
+        <div class="panel-header">Social Actions <span onclick="togglePanel('panel-socials')" style="cursor:pointer">×</span></div>
+        <div id="socials-area" class="panel-content"></div>
+    </div>
+
+    <div id="panel-editor" class="overlay-panel" style="width: 80%; height: 80%; top: 10%; left: 10%; border: 2px solid #2196F3; z-index: 2000;">
         <div class="panel-header" style="background: #1565C0;">
             <span id="editor-title">Editor</span>
             <span onclick="togglePanel('panel-editor')" style="cursor:pointer">×</span>
         </div>
-        <div class="panel-content" style="display:flex; flex-direction:column; gap:10px; padding: 0;">
+        <div class="panel-content" style="display:flex; flex-direction:column; gap:0; padding: 0;">
             <div id="editor-container" style="flex:1; background:#1e1e1e;"></div>
             <div style="background:#252526; padding:10px 15px; display:flex; gap:10px; border-top:1px solid #333;">
                 <button class="tool-btn" onclick="saveEditorFile()" style="background:#2196F3; border-color:#2196F3;">💾 Save (Ctrl+S)</button>
@@ -111,10 +115,6 @@
             </div>
         </div>
     </div>
-    <div id="panel-socials" class="overlay-panel">
-        <div class="panel-header">Social Actions <span onclick="togglePanel('panel-socials')" style="cursor:pointer">×</span></div>
-        <div id="socials-area" class="panel-content"></div>
-    </div>
 
     <div id="panel-import" class="overlay-panel">
         <div class="panel-header">Import Emoji Map <span onclick="togglePanel('panel-import')" style="cursor:pointer">×</span></div>
@@ -158,11 +158,6 @@
     <div id="send-area">
         <div id="suggestion-box"></div>
         <div id="history-box"></div>
-        
-        <!-- 🚀 最終防線指令輸入框：
-             1. 始終維持 type="text"，徹底讓 Chrome 失去掃描目標
-             2. 使用 readonly 動態切換法，阻斷自動填充引擎的掛接
-        -->
         <input id="msg" 
                type="text"
                placeholder="Type command..." 
@@ -181,6 +176,8 @@
 <script>
     let ws, historyCmds = [], histIdx = -1, msgInput, suggestIdx = -1, isInputHidden = false;
     let mcData = null, mcMode = false, mcAction = 'dig', mcZoom = 32;
+    let monacoEditor = null, currentEditPath = "";
+
     const mcColors = {
         grass:'#4CAF50', dirt:'#795548', stone:'#9E9E9E',
         wood:'#8D6E63', log:'#5D4037', planks:'#FFCC80',
@@ -189,6 +186,39 @@
         brick:'#BF360C', player:'#2196F3', self:'#FFEB3B'
     };
 
+    // Monaco 初始化 (安全檢查)
+    if (typeof require !== "undefined") {
+        require.config({ paths: { 'vs': 'https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs' }});
+        require(['vs/editor/editor.main'], function() {
+            monaco.languages.register({ id: 'lpc' });
+            monaco.languages.setMonarchTokensProvider('lpc', {
+                tokenizer: {
+                    root: [
+                        [/[a-zA-Z_]\w*/, {
+                            cases: {
+                                'inherit|mixed|object|string|int|float|mapping|array|void|private|protected|public|nomask|varargs|virtual|static|status': 'keyword',
+                                '@default': 'identifier'
+                            }
+                        }],
+                        { include: 'vs/basic-languages/cpp/cpp' }
+                    ]
+                }
+            });
+
+            monacoEditor = monaco.editor.create(document.getElementById('editor-container'), {
+                value: '',
+                theme: 'vs-dark',
+                automaticLayout: true,
+                fontSize: 16,
+                minimap: { enabled: true }
+            });
+
+            monacoEditor.addCommand(monaco.KeyMod.CtrlCmd | monaco.KeyCode.KeyS, function() {
+                saveEditorFile();
+            });
+        });
+    }
+
     window.onload = () => {
         msgInput = document.getElementById("msg");
         initLocalization();
@@ -199,12 +229,10 @@
 
     function togglePanel(id, btn) {
         const panel = document.getElementById(id);
+        if (!panel) return;
         const isActive = panel.classList.contains('active');
-        
-        // 關閉所有面板
         document.querySelectorAll('.overlay-panel').forEach(p => p.classList.remove('active'));
         document.querySelectorAll('.tool-btn').forEach(b => b.classList.remove('active'));
-
         if (!isActive) {
             panel.classList.add('active');
             if (btn) btn.classList.add('active');
@@ -213,7 +241,7 @@
 
     function initLocalization() {
         const isZH = (navigator.language || navigator.userLanguage).startsWith('zh');
-        if (isZH) {
+        if (isZH && msgInput) {
             msgInput.placeholder = "點擊查看歷史或輸入指令...";
             document.getElementById("btn-send").innerText = "發送";
             document.querySelector('.tool-btn[onclick*="info"]').innerText = "📊 狀態";
@@ -228,18 +256,19 @@
         ws = new WebSocket(protocol + "//" + location.host + "/ws");
         ws.onmessage = (e) => {
             const msg = JSON.parse(e.data);
-            if (msg.type === "mud_text" || msg.type === "mud_html" || msg.type === "edit_file" || msg.type === "chat") {
-                const p = (msg.type === "chat" ? (msg.username + ": " + msg.payload) : msg.payload).trim();
+            // 🚀 核心優化：處理任何包含 payload 的訊息
+            if (msg.payload !== undefined && msg.payload !== null) {
+                const p = (msg.type === "chat" ? (msg.username + ": " + msg.payload) : msg.payload);
                 if (p === "__INPUT_HIDDEN__") {
                     isInputHidden = true;
-                    msgInput.style.webkitTextSecurity = "disc";
+                    if (msgInput) msgInput.style.webkitTextSecurity = "disc";
                     return;
                 }
                 
                 if (msg.type === "mud_html") { appendRaw(p); return; }
                 if (msg.type === "edit_file") { openEditor(JSON.parse(p)); return; }
 
-                if (p.startsWith("{") && p.endsWith("}")) {
+                if (typeof p === "string" && p.startsWith("{") && p.endsWith("}")) {
                     try { handleUIData(JSON.parse(p)); } catch (err) { handleMultiJSON(p); }
                 } else {
                     appendLook(p);
@@ -258,10 +287,11 @@
     function handleUIData(ui) {
         if (ui.ui === "score") {
             const d = ui.data;
-            document.getElementById("info-area").innerHTML = `
+            const area = document.getElementById("info-area");
+            if (area) area.innerHTML = `
                 <div class="stat-box"><span class="stat-label">❤️ HP</span><span class="stat-value">${d.hp}/${d.max_hp}</span></div>
                 <div class="stat-box"><span class="stat-label">✨ MP</span><span class="stat-value">${d.mp}/${d.max_mp}</span></div>
-                <div class="stat-box"><span class="stat-label">💰 GOLD</span><span class="stat-value">${parseColorTags(d.money)}</span></div>
+                <div class="stat-box"><span class="stat-label">💰 GOLD</span><span class="stat-value">${parseOutput(d.money)}</span></div>
             `;
         } else if (ui.ui === "cmd_list") { renderCmdList(ui.data, ui.title); window.allCommands = ui.data; }
         else if (ui.ui === "socials") { renderSocials(ui.data, ui.title); window.allSocials = ui.data; }
@@ -271,8 +301,34 @@
         else if (ui.ui === "mc_exit") toggleMCMode(false); 
     }
 
+    function openEditor(data) {
+        currentEditPath = data.path;
+        document.getElementById("editor-title").innerText = "📝 Editing: " + data.path;
+        if (monacoEditor) {
+            const ext = data.path.split('.').pop();
+            const lang = (ext === 'c' || ext === 'h') ? 'cpp' : 'plaintext';
+            monaco.editor.setModelLanguage(monacoEditor.getModel(), lang);
+            monacoEditor.setValue(data.content);
+        }
+        document.getElementById("editor-status").innerText = "";
+        togglePanel('panel-editor');
+        if (monacoEditor) setTimeout(() => monacoEditor.focus(), 100);
+    }
+
+    function saveEditorFile() {
+        if (!currentEditPath || !monacoEditor) return;
+        const content = monacoEditor.getValue();
+        ws.send(JSON.stringify({ 
+            type: "save_file", 
+            payload: JSON.stringify({ path: currentEditPath, content: content }) 
+        }));
+        document.getElementById("editor-status").innerText = "Saving...";
+        setTimeout(() => { document.getElementById("editor-status").innerText = "Saved at " + new Date().toLocaleTimeString(); }, 500);
+    }
+
     function renderCmdList(cats, title) {
         const area = document.getElementById("cmds-area");
+        if (!area) return;
         area.innerHTML = "";
         for (let c in cats) {
             let group = document.createElement("div"); group.className = "cmd-group";
@@ -290,6 +346,7 @@
 
     function renderSocials(socials, title) {
         const grid = document.getElementById("socials-area");
+        if (!grid) return;
         grid.innerHTML = `<div id='socials-grid'></div>`;
         const container = document.getElementById("socials-grid");
         for (let k in socials) {
@@ -302,21 +359,23 @@
     function handleMCMap(data) {
         if (!mcMode) toggleMCMode(true);
         mcData = data;
-        if (data.world_name) {
-            document.querySelector("#mc-ui span").innerText = data.world_name;
-        }
+        const span = document.querySelector("#mc-ui span");
+        if (span && data.world_name) span.innerText = data.world_name;
         renderMCMap();
     }
 
     function toggleMCMode(on) {
         mcMode = on;
-        document.getElementById('mc-container').style.display = on ? 'flex' : 'none';
-        document.getElementById('look-content').style.display = on ? 'none' : 'block';
+        const mc = document.getElementById('mc-container');
+        const look = document.getElementById('look-content');
+        if (mc) mc.style.display = on ? 'flex' : 'none';
+        if (look) look.style.display = on ? 'none' : 'block';
         if (on) initMCCanvas();
     }
 
     function initMCCanvas() {
         const canvas = document.getElementById('mc-canvas');
+        if (!canvas) return;
         canvas.width = canvas.offsetWidth; canvas.height = canvas.offsetHeight;
         canvas.onmousedown = (e) => {
             if (!mcData) return;
@@ -349,9 +408,7 @@
         const selfPos = players[self_id] || [Math.floor(width/2), Math.floor(height/2)];
         const cw = canvas.width, ch = canvas.height;
         const centerX = cw / 2, centerY = ch / 2;
-
         ctx.fillStyle = '#0d1b2a'; ctx.fillRect(0, 0, cw, ch);
-
         const wx0 = centerX + (0 - selfPos[0]) * mcZoom;
         const wy0 = centerY - (0 - selfPos[1]) * mcZoom;
         const wx1 = centerX + (width - selfPos[0]) * mcZoom;
@@ -360,7 +417,6 @@
         ctx.setLineDash([6, 4]);
         ctx.strokeRect(wx0, wy1, wx1 - wx0, wy0 - wy1);
         ctx.setLineDash([]);
-
         if (mcZoom >= 20) {
             ctx.strokeStyle = 'rgba(255,255,255,0.04)'; ctx.lineWidth = 0.5;
             const startX = Math.floor((0 - centerX / mcZoom) + selfPos[0]);
@@ -374,7 +430,6 @@
                 ctx.beginPath(); ctx.moveTo(0, sy); ctx.lineTo(cw, sy); ctx.stroke();
             }
         }
-
         for (let key in blocks) {
             const [bx, by] = key.split(',').map(Number);
             const sx = centerX + (bx - selfPos[0]) * mcZoom;
@@ -386,14 +441,7 @@
                 ctx.strokeStyle = 'rgba(0,0,0,0.25)'; ctx.lineWidth = 0.5;
                 ctx.strokeRect(sx, sy - mcZoom, mcZoom, mcZoom);
             }
-            if (mcZoom >= 40) {
-                ctx.fillStyle = 'rgba(255,255,255,0.7)';
-                ctx.font = Math.max(8, mcZoom/4) + 'px monospace';
-                ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
-                ctx.fillText(blocks[key], sx + mcZoom/2, sy - mcZoom/2);
-            }
         }
-
         for (let pid in players) {
             const ppos = players[pid];
             const sx = centerX + (ppos[0] - selfPos[0]) * mcZoom;
@@ -409,28 +457,12 @@
             ctx.textAlign = 'center'; ctx.textBaseline = 'bottom';
             ctx.fillText(pid, sx + mcZoom/2, sy - mcZoom - 2);
         }
-
-        if (mcData.npcs) {
-            for (let nid in mcData.npcs) {
-                const npos = mcData.npcs[nid];
-                const sx = centerX + (npos[0] - selfPos[0]) * mcZoom;
-                const sy = centerY - (npos[1] - selfPos[1]) * mcZoom;
-                const icon = npos[2] || '👾';
-                ctx.font = Math.max(12, mcZoom * 0.8) + 'px sans-serif';
-                ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
-                ctx.fillText(icon, sx + mcZoom/2, sy - mcZoom/2);
-            }
-        }
-
         ctx.fillStyle = 'rgba(0,0,0,0.55)'; ctx.fillRect(6, 6, 160, 22);
         ctx.fillStyle = '#adf'; ctx.font = '12px monospace'; ctx.textAlign = 'left'; ctx.textBaseline = 'top';
         ctx.fillText(`pos (${selfPos[0]}, ${selfPos[1]})  zoom ${mcZoom}px`, 10, 10);
     }
 
-    function promptImportMap() {
-        togglePanel('panel-import');
-    }
-
+    function promptImportMap() { togglePanel('panel-import'); }
     function submitMapImport() {
         const input = document.getElementById("import-area").value;
         if (input) {
@@ -440,7 +472,7 @@
     }
 
     function renderMiniMap(d) {
-        const m = document.getElementById("minimap-container"); if (!d.grid) return;
+        const m = document.getElementById("minimap-container"); if (!m || !d.grid) return;
         let html = ""; const exits = new Set(d.exits || []);
         for (let y = 0; y < d.grid.length; y++) {
             for (let x = 0; x < d.grid[y].length; x++) {
@@ -454,103 +486,6 @@
         m.innerHTML = html; m.style.display = "grid";
     }
 
-    
-    let currentEditPath = "";
-    function openEditor(data) {
-        currentEditPath = data.path;
-        document.getElementById("editor-title").innerText = "📝 Editing: " + data.path;
-        document.getElementById("editor-area").value = data.content;
-        document.getElementById("editor-status").innerText = "";
-        togglePanel('panel-editor');
-        setTimeout(() => document.getElementById("editor-area").focus(), 100);
-    }
-
-    function saveEditorFile() {
-        if (!currentEditPath) return;
-        const content = document.getElementById("editor-area").value;
-        // 目前暫時透過指令方式存檔 (Phase 1)，Phase 2 改用專屬 save_file type
-        // 為了讓 Driver 知道是網頁存檔，我們定義一個隱藏指令
-        ws.send(JSON.stringify({ type: "cmd", payload: "__save_web_file__ " + currentEditPath + "\n" + content }));
-        document.getElementById("editor-status").innerText = "Saving...";
-        setTimeout(() => { document.getElementById("editor-status").innerText = "Saved at " + new Date().toLocaleTimeString(); }, 500);
-    }
-
-    // 監聽 Ctrl+S
-    document.addEventListener('keydown', (e) => {
-        if (e.ctrlKey && e.key === 's') {
-            const editor = document.getElementById('panel-editor');
-            if (editor.classList.contains('active')) {
-                e.preventDefault();
-                saveEditorFile();
-            }
-        }
-    });
-
-    
-    let monacoEditor = null;
-    let currentEditPath = "";
-
-    // 初始化 Monaco
-    require.config({ paths: { 'vs': 'https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.44.0/min/vs' }});
-    require(['vs/editor/editor.main'], function() {
-        // 定義 LPC 語法高亮 (基於 C 擴充)
-        monaco.languages.register({ id: 'lpc' });
-        monaco.languages.setMonarchTokensProvider('lpc', {
-            tokenizer: {
-                root: [
-                    [/[a-zA-Z_]\w*/, {
-                        cases: {
-                            'inherit|mixed|object|string|int|float|mapping|array|void|private|protected|public|nomask|varargs|virtual|static|status': 'keyword',
-                            '@default': 'identifier'
-                        }
-                    }],
-                    { include: 'vs/basic-languages/cpp/cpp' } // 繼承 C++ 基礎
-                ]
-            }
-        });
-
-        monacoEditor = monaco.editor.create(document.getElementById('editor-container'), {
-            value: '',
-            language: 'cpp', // 暫時先用 cpp，之後改進 lpc
-            theme: 'vs-dark',
-            automaticLayout: true,
-            fontSize: 16,
-            minimap: { enabled: true }
-        });
-
-        // 快捷鍵支援
-        monacoEditor.addCommand(monaco.KeyMod.CtrlCmd | monaco.KeyCode.KeyS, function() {
-            saveEditorFile();
-        });
-    });
-
-    function openEditor(data) {
-        currentEditPath = data.path;
-        document.getElementById("editor-title").innerText = "📝 Editing: " + data.path;
-        
-        if (monacoEditor) {
-            const ext = data.path.split('.').pop();
-            const lang = (ext === 'c' || ext === 'h') ? 'cpp' : 'plaintext';
-            monaco.editor.setModelLanguage(monacoEditor.getModel(), lang);
-            monacoEditor.setValue(data.content);
-        }
-        
-        document.getElementById("editor-status").innerText = "";
-        togglePanel('panel-editor');
-        if (monacoEditor) setTimeout(() => monacoEditor.focus(), 100);
-    }
-
-    function saveEditorFile() {
-        if (!currentEditPath || !monacoEditor) return;
-        const content = monacoEditor.getValue();
-        ws.send(JSON.stringify({ 
-            type: "save_file", 
-            payload: JSON.stringify({ path: currentEditPath, content: content }) 
-        }));
-        document.getElementById("editor-status").innerText = "Saving...";
-        setTimeout(() => { document.getElementById("editor-status").innerText = "Saved at " + new Date().toLocaleTimeString(); }, 500);
-    }
-
     function sendCmd(cmd) {
         if (cmd !== "") {
             if (historyCmds.length === 0 || historyCmds[historyCmds.length - 1] !== cmd) {
@@ -559,14 +494,14 @@
         }
         histIdx = historyCmds.length;
         hideHistory();
-        ws.send(JSON.stringify({ type: "cmd", payload: cmd }));
+        if (ws) ws.send(JSON.stringify({ type: "cmd", payload: cmd }));
         const echo = (isInputHidden ? "******" : cmd).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;");
         appendRaw("<span style='color:#888'>&gt;&gt; " + echo + "</span>");
     }
 
-    // appendRaw: 已是 HTML，直接插入，不做 escape 或 parseOutput
     function appendRaw(html) {
         const area = document.getElementById("look-area"), content = document.getElementById("look-content");
+        if (!area || !content) return;
         const div = document.createElement("div");
         div.style.whiteSpace = "pre-wrap";
         div.innerHTML = html;
@@ -575,95 +510,64 @@
     }
 
     function appendLook(t) {
-        const area = document.getElementById("look-area"), content = document.getElementById("look-content");
-        // 所有輸入一律視為純文字，先 escape HTML，再交給 parseOutput 處理
-        let p = t
-            .replace(/&/g, "&amp;")
-            .replace(/</g, "&lt;")
-            .replace(/>/g, "&gt;");
-        p = parseOutput(p);
-        // [顯示文字|指令] 可點擊連結
-        p = p.replace(/\[(.*?)\]/g, (m, p1) => {
+        if (typeof t !== "string") return;
+        const p1 = t.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
+        const p2 = parseOutput(p1);
+        const p3 = p2.replace(/\[(.*?)\]/g, (m, p1) => {
             let pts = p1.split('|'), d = pts[0], c = pts.length > 1 ? pts[1] : pts[0];
             return `<span class="cmd-link" onclick="sendCmd('${c.replace(/'/g, "\\'")}')">${d}</span>`;
         });
-        const div = document.createElement("div");
-        div.style.whiteSpace = "pre-wrap";
-        div.innerHTML = p;
-        content.appendChild(div);
-        setTimeout(() => { area.scrollTop = area.scrollHeight; }, 10);
+        appendRaw(p3);
     }
 
-    // parseOutput: 統一處理所有輸出轉換
-    // 順序：ANSI → $TAG$ → {tag} → markdown
-    // 輸入已是 HTML-escaped 純文字，輸出是帶 HTML 標籤的字串。
     function parseOutput(t) {
-        let res = t;
-        let open = 0;
-        // ── 0. 標題佔位符（最先跑，在所有 HTML 生成之前）────────────────────
+        if (typeof t !== "string") return t;
+        let res = t, open = 0;
         res = res.replace(/###([^#\n]+)###/g, "\x00H3\x00$1\x00/H3\x00");
         res = res.replace(/##([^#\n]+)##/g,   "\x00H2\x00$1\x00/H2\x00");
         res = res.replace(/#([^#\n]+)#/g,     "\x00H1\x00$1\x00/H1\x00");
 
-
-        // ── 1. ANSI escape code ─────────────────────────────────────────────
-        // 串連式用法：HIR + "文字" + NOR 展開後是 \x1b[1;31m文字\x1b[0m
         const ansiMap = {
-            "0;30":"#555",    "0;31":"#ff5555", "0;32":"#55ff55", "0;33":"#ffff55",
-            "0;34":"#5555ff", "0;35":"#ff55ff", "0;36":"#55ffff", "0;37":"#ffffff",
-            "30":"#555",      "31":"#ff5555",   "32":"#55ff55",   "33":"#ffff55",
-            "34":"#5555ff",   "35":"#ff55ff",   "36":"#55ffff",   "37":"#ffffff",
-            "1;30":"#888",    "1;31":"#ff7777", "1;32":"#77ff77", "1;33":"#ffff77",
-            "1;34":"#7777ff", "1;35":"#ff77ff", "1;36":"#77ffff", "1;37":"#ffffff",
+            "0;30":"#555", "0;31":"#ff5555", "0;32":"#55ff55", "0;33":"#ffff55", "0;34":"#5555ff", "0;35":"#ff55ff", "0;36":"#55ffff", "0;37":"#ffffff",
+            "30":"#555", "31":"#ff5555", "32":"#55ff55", "33":"#ffff55", "34":"#5555ff", "35":"#ff55ff", "36":"#55ffff", "37":"#ffffff",
+            "1;30":"#888", "1;31":"#ff7777", "1;32":"#77ff77", "1;33":"#ffff77", "1;34":"#7777ff", "1;35":"#ff77ff", "1;36":"#77ffff", "1;37":"#ffffff",
             "41;1":"bg:#a33", "42;1":"bg:#3a3", "43;1":"bg:#aa3",
             "44;1":"bg:#33a", "45;1":"bg:#a3a", "46;1":"bg:#3aa", "47;1":"bg:#aaa",
-            "40":"bg:#000",   "41":"bg:#500",   "42":"bg:#050",   "43":"bg:#550",
-            "44":"bg:#005",   "45":"bg:#505",   "46":"bg:#055",   "47":"bg:#555",
+            "40":"bg:#000", "41":"bg:#500", "42":"bg:#050", "43":"bg:#550", "44":"bg:#005", "45":"bg:#505", "46":"bg:#055", "47":"bg:#555",
             "1":"bold", "4":"underline", "5":"blink", "7":"reverse", "1;7":"bold-reverse",
         };
         res = res.replace(/\x1b\[([0-9;]+)m/g, (m, code) => {
             if (code === "0" || code === "2;37;0") {
                 let s = ""; while (open > 0) { s += "</span>"; open--; } return s;
             }
-            const v = ansiMap[code];
-            if (!v) return "";
+            const v = ansiMap[code]; if (!v) return "";
             open++;
-            if (v === "bold")         return `<span style="font-weight:bold">`;
-            if (v === "underline")    return `<span style="text-decoration:underline">`;
-            if (v === "blink")        return `<span style="animation:blink 1s step-end infinite">`;
-            if (v === "reverse")      return `<span style="filter:invert(1)">`;
+            if (v === "bold") return `<span style="font-weight:bold">`;
+            if (v === "underline") return `<span style="text-decoration:underline">`;
+            if (v === "blink") return `<span style="animation:blink 1s step-end infinite">`;
+            if (v === "reverse") return `<span style="filter:invert(1)">`;
             if (v === "bold-reverse") return `<span style="font-weight:bold;filter:invert(1)">`;
-            if (v.startsWith("bg:"))  return `<span style="background:${v.slice(3)}">`;
+            if (v.startsWith("bg:")) return `<span style="background:${v.slice(3)}">`;
             return `<span style="color:${v}">`;
         });
         while (open > 0) { res += "</span>"; open--; }
         open = 0;
 
-        // ── 2. $TAG$ 嵌入式顏色巨集 ─────────────────────────────────────────
-        // 嵌入式用法："xxx$HIY$yyy$NOR$zzz"
-        // $ 不是 HTML 特殊字元，escape 後仍是原樣
         if (res.includes("$")) {
             const tagMap = {
-                "$BLK$":"#555",     "$RED$":"#ff5555", "$GRN$":"#55ff55", "$YEL$":"#ffff55",
-                "$BLU$":"#5555ff",  "$MAG$":"#ff55ff", "$CYN$":"#55ffff", "$WHT$":"#ffffff",
-                "$HIK$":"#888",
-                "$HIR$":"#ff7777",  "$HIG$":"#77ff77", "$HIY$":"#ffff77",
-                "$HIB$":"#7777ff",  "$HIM$":"#ff77ff", "$HIC$":"#77ffff", "$HIW$":"#ffffff",
-                "$U$":"underline",  "$REV$":"reverse", "$HIREV$":"bold-reverse",
-                "$BLINK$":"blink",  "$BOLD$":"bold",
+                "$BLK$":"#555", "$RED$":"#ff5555", "$GRN$":"#55ff55", "$YEL$":"#ffff55", "$BLU$":"#5555ff", "$MAG$":"#ff55ff", "$CYN$":"#55ffff", "$WHT$":"#ffffff",
+                "$HIK$":"#888", "$HIR$":"#ff7777", "$HIG$":"#77ff77", "$HIY$":"#ffff77", "$HIB$":"#7777ff", "$HIM$":"#ff77ff", "$HIC$":"#77ffff", "$HIW$":"#ffffff",
+                "$U$":"underline", "$REV$":"reverse", "$HIREV$":"bold-reverse", "$BLINK$":"blink", "$BOLD$":"bold",
             };
             res = res.replace(/\$([A-Z0-9]+)\$/g, (m, name) => {
                 const key = "$" + name + "$";
-                if (key === "$NOR$") {
-                    let s = ""; while (open > 0) { s += "</span>"; open--; } return s;
-                }
-                const v = tagMap[key];
-                if (!v) return m;
+                if (key === "$NOR$") { let s = ""; while (open > 0) { s += "</span>"; open--; } return s; }
+                const v = tagMap[key]; if (!v) return m;
                 open++;
-                if (v === "bold")         return `<span style="font-weight:bold">`;
-                if (v === "underline")    return `<span style="text-decoration:underline">`;
-                if (v === "blink")        return `<span style="animation:blink 1s step-end infinite">`;
-                if (v === "reverse")      return `<span style="filter:invert(1)">`;
+                if (v === "bold") return `<span style="font-weight:bold">`;
+                if (v === "underline") return `<span style="text-decoration:underline">`;
+                if (v === "blink") return `<span style="animation:blink 1s step-end infinite">`;
+                if (v === "reverse") return `<span style="filter:invert(1)">`;
                 if (v === "bold-reverse") return `<span style="font-weight:bold;filter:invert(1)">`;
                 return `<span style="color:${v}">`;
             });
@@ -671,54 +575,32 @@
             open = 0;
         }
 
-        // ── 3. {r}{g} 舊式標籤（向下相容）──────────────────────────────────
         if (res.includes("{")) {
-            const oldMap = {
-                "r":"#ff5555","g":"#55ff55","b":"#5555ff","y":"#ffff55",
-                "c":"#55ffff","m":"#ff55ff","w":"#ffffff","gr":"#888",
-            };
+            const oldMap = { "r":"#ff5555","g":"#55ff55","b":"#5555ff","y":"#ffff55","c":"#55ffff","m":"#ff55ff","w":"#ffffff","gr":"#888" };
             res = res.replace(/\{([a-z]+)\}/g, (m, tag) => {
-                if (tag === "n") {
-                    let s = ""; while (open > 0) { s += "</span>"; open--; } return s;
-                }
-                if (oldMap[tag]) { open++; return `<span style="color:${oldMap[tag]}">`;  }
+                if (tag === "n") { let s = ""; while (open > 0) { s += "</span>"; open--; } return s; }
+                if (oldMap[tag]) { open++; return `<span style="color:${oldMap[tag]}">`; }
                 return m;
             });
             while (open > 0) { res += "</span>"; open--; }
         }
 
-        // ── 4. Markdown-like 語法（標題已在步驟 0 標記）───────────────────────
-        // 分隔線（單獨一行）
         res = res.replace(/^---\s*$/gm, "<hr style='border:none;border-top:1px solid #444;margin:4px 0'>");
         res = res.replace(/^===\s*$/gm, "<hr style='border:none;border-top:2px double #888;margin:4px 0'>");
-
-        // 刪除線
-        res = res.replace(/~~([^\n~]+)~~/g,
-            "<span style='text-decoration:line-through;opacity:0.7'>$1</span>");
-
-        // 粗斜體 *** 先比對
-        res = res.replace(/\*{3}([^\n*]+)\*{3}/g,
-            "<span style='font-weight:bold;font-style:italic;color:#ffe'>$1</span>");
-        // 粗體
-        res = res.replace(/\*{2}([^\n*]+)\*{2}/g,
-            "<span style='font-weight:bold;color:#fff'>$1</span>");
-        // 斜體
-        res = res.replace(/\*([^\n*]+)\*/g,
-            "<span style='font-style:italic;color:#cce'>$1</span>");
-
-        // 佔位符 → 標題 HTML（inline span，不換行）
-        res = res.replace(/\x00H3\x00([^\x00]*)\x00\/H3\x00/g,
-            "<span style='font-size:1.0em;font-weight:bold;color:rgb(170,170,170)'>$1</span>");
-        res = res.replace(/\x00H2\x00([^\x00]*)\x00\/H2\x00/g,
-            "<span style='font-size:1.2em;font-weight:bold;color:rgb(221,221,221)'>$1</span>");
-        res = res.replace(/\x00H1\x00([^\x00]*)\x00\/H1\x00/g,
-            "<span style='font-size:1.4em;font-weight:bold;color:rgb(255,255,119)'>$1</span>");
+        res = res.replace(/~~([^\n~]+)~~/g, "<span style='text-decoration:line-through;opacity:0.7'>$1</span>");
+        res = res.replace(/\*{3}([^\n*]+)\*{3}/g, "<span style='font-weight:bold;font-style:italic;color:#ffe'>$1</span>");
+        res = res.replace(/\*{2}([^\n*]+)\*{2}/g, "<span style='font-weight:bold;color:#fff'>$1</span>");
+        res = res.replace(/\*([^\n*]+)\*/g, "<span style='font-style:italic;color:#cce'>$1</span>");
+        res = res.replace(/\x00H3\x00([^\x00]*)\x00\/H3\x00/g, "<span style='font-size:1.0em;font-weight:bold;color:rgb(170,170,170)'>$1</span>");
+        res = res.replace(/\x00H2\x00([^\x00]*)\x00\/H2\x00/g, "<span style='font-size:1.2em;font-weight:bold;color:rgb(221,221,221)'>$1</span>");
+        res = res.replace(/\x00H1\x00([^\x00]*)\x00\/H1\x00/g, "<span style='font-size:1.4em;font-weight:bold;color:rgb(255,255,119)'>$1</span>");
 
         return res;
     }
 
     function initStars() {
         const area = document.getElementById("look-area");
+        if (!area) return;
         for (let i = 0; i < 40; i++) {
             const s = document.createElement("div"); s.className = "star";
             const sz = (Math.random() * 2 + 1) + "px"; s.style.width = sz; s.style.height = sz;
@@ -728,11 +610,8 @@
     }
 
     function initInputHandlers() {
-        msgInput.onclick = (e) => {
-            e.stopPropagation();
-            if (historyCmds.length > 0) showHistory();
-        };
-
+        if (!msgInput) return;
+        msgInput.onclick = (e) => { e.stopPropagation(); if (historyCmds.length > 0) showHistory(); };
         window.addEventListener('keydown', (e) => {
             if (mcMode) {
                 if (document.activeElement === msgInput && msgInput.value !== "" && e.key !== "Enter") return;
@@ -740,7 +619,6 @@
                 let dir = { w: 'n', s: 's', a: 'w', d: 'e', arrowup: 'n', arrowdown: 's', arrowleft: 'w', arrowright: 'e' }[key];
                 if (dir) { e.preventDefault(); sendCmd(`mc move ${dir}`); return; }
             }
-
             if (document.activeElement === msgInput) {
                 if (e.key === "ArrowUp") { e.preventDefault(); if (histIdx > 0) { histIdx--; msgInput.value = historyCmds[histIdx]; } }
                 else if (e.key === "ArrowDown") { e.preventDefault(); if (histIdx < historyCmds.length - 1) { histIdx++; msgInput.value = historyCmds[histIdx]; } else { histIdx = historyCmds.length; msgInput.value = ""; } }
@@ -752,26 +630,22 @@
 
     function showHistory() {
         const box = document.getElementById("history-box");
+        if (!box) return;
         let html = '<div class="history-header"><span>Command History</span><span onclick="hideHistory()" style="cursor:pointer">×</span></div>';
         const recent = historyCmds.slice(-10).reverse();
-        recent.forEach(cmd => {
-            html += `<div class="history-item" onclick="useHistory('${cmd.replace(/'/g, "\\'")}')">${cmd}</div>`;
-        });
+        recent.forEach(cmd => { html += `<div class="history-item" onclick="useHistory('${cmd.replace(/'/g, "\\'")}')">${cmd}</div>`; });
         box.innerHTML = html; box.style.display = "block";
     }
 
-    function hideHistory() { document.getElementById("history-box").style.display = "none"; }
-    function useHistory(cmd) { msgInput.value = cmd; hideHistory(); msgInput.focus(); }
+    function hideHistory() { const box = document.getElementById("history-box"); if (box) box.style.display = "none"; }
+    function useHistory(cmd) { if (msgInput) { msgInput.value = cmd; hideHistory(); msgInput.focus(); } }
 
     function sendMsg() { 
-        const v = msgInput.value;
-        sendCmd(v); 
-        msgInput.value = ""; 
-        isInputHidden = false;
-        msgInput.style.webkitTextSecurity = "none";
-        msgInput.focus(); 
+        if (!msgInput) return;
+        const v = msgInput.value; sendCmd(v); msgInput.value = ""; 
+        isInputHidden = false; msgInput.style.webkitTextSecurity = "none"; msgInput.focus(); 
     }
-    function fillInput(c) { msgInput.value = c + " "; msgInput.focus(); }
+    function fillInput(c) { if (msgInput) { msgInput.value = c + " "; msgInput.focus(); } }
     function handleNatureData(d) {
         document.body.style.background = d.is_day ? "linear-gradient(to bottom, #1a2a6c, #b21f1f, #fdbb2d)" : "linear-gradient(to bottom, #000000, #0f0c29, #000000)";
     }
diff --git a/go.mod b/go.mod
index a4689018..534a08a1 100644
--- a/go.mod
+++ b/go.mod
@@ -7,7 +7,6 @@ require (
 	github.com/gorilla/websocket v1.5.3
 	github.com/pion/stun v0.6.1
 	github.com/pion/webrtc/v4 v4.2.12
-	golang.org/x/crypto v0.48.0
 )
 
 require (
@@ -29,6 +28,7 @@ require (
 	github.com/pion/transport/v4 v4.0.1 // indirect
 	github.com/pion/turn/v5 v5.0.3 // indirect
 	github.com/wlynxg/anet v0.0.5 // indirect
+	golang.org/x/crypto v0.48.0 // indirect
 	golang.org/x/net v0.50.0 // indirect
 	golang.org/x/sys v0.41.0 // indirect
 	golang.org/x/time v0.14.0 // indirect
-- 
2.43.0

